% ══════════════════════════════════════════════════════════════════════════
% After Deep CFR — Pluribus, ReBeL, Player of Games, AlphaHoldem
% Companion volume to deep_cfr_tutorial.pdf, same pedagogical style.
% Build:  pdflatex modern_poker_ai_tutorial.tex   (run twice for refs)
% ══════════════════════════════════════════════════════════════════════════
\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=2.2cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc,fit,backgrounds}
\usepackage{listings}
\usepackage{tcolorbox}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=MidnightBlue,urlcolor=MidnightBlue,citecolor=MidnightBlue]{hyperref}

% ─── Palette (same as the Deep CFR tutorial) ────────────────────────────────
\definecolor{accent}{RGB}{25,55,110}
\definecolor{heroG}{RGB}{20,110,60}
\definecolor{villR}{RGB}{160,40,40}
\definecolor{memO}{RGB}{190,110,10}
\definecolor{codebg}{RGB}{246,247,249}
\definecolor{codekw}{RGB}{0,80,160}
\definecolor{codecomment}{RGB}{100,120,100}
\definecolor{codestr}{RGB}{150,60,20}

\lstdefinestyle{py}{
  language=Python,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codekw}\bfseries,
  commentstyle=\color{codecomment}\itshape,
  stringstyle=\color{codestr},
  backgroundcolor=\color{codebg},
  frame=single, rulecolor=\color{black!25},
  framesep=6pt, xleftmargin=10pt, xrightmargin=4pt,
  showstringspaces=false,
  columns=fullflexible, keepspaces=true,
  breaklines=true
}

\newtcolorbox{intuition}{
  colback=blue!4, colframe=accent, boxrule=0.8pt, arc=2mm,
  title=\textbf{Intuition}, fonttitle=\color{white},
  left=6pt, right=6pt, top=4pt, bottom=4pt}
\newtcolorbox{trap}{
  colback=red!4, colframe=villR, boxrule=0.8pt, arc=2mm,
  title=\textbf{Watch out}, fonttitle=\color{white},
  left=6pt, right=6pt, top=4pt, bottom=4pt}
\newtcolorbox{defbox}{
  colback=black!3, colframe=black!50, boxrule=0.6pt, arc=2mm,
  left=6pt, right=6pt, top=4pt, bottom=4pt}

\setlength{\parskip}{4pt}
\setlength{\parindent}{0pt}

\title{\bfseries After Deep CFR: four modern poker AIs\\[2mm]
\large Pluribus, ReBeL, Player of Games, AlphaHoldem --- a pedagogical tour\\[1mm]
\normalsize Companion volume to \emph{Deep CFR, explained}}
\author{PokerTrainer documentation}
\date{June 2026}

\begin{document}
\maketitle

\begin{abstract}
The companion tutorial \emph{Deep CFR, explained} covered the theory line from
regret matching to Deep CFR. This volume takes the next step and dissects four
landmark systems that followed: \textbf{Pluribus} (superhuman six-player poker
with \emph{no} neural network), \textbf{ReBeL} (reinforcement learning $+$
search on \emph{belief states}), \textbf{Player of Games} (one sound algorithm
for chess, Go \emph{and} poker), and \textbf{AlphaHoldem} (end-to-end deep RL,
no search at all). For each: the one core idea, how it works mechanically
(diagrams $+$ Python pseudo-code), what it achieved, and what it costs.
A final section compares all of them --- including Deep CFR --- on one table and
draws lessons for this project.
\end{abstract}

\tableofcontents
\newpage

% ════════════════════════════════════════════════════════════════════════════
\section{The design space: one map before four deep dives}
% ════════════════════════════════════════════════════════════════════════════

Every poker AI answers two independent questions. \textbf{(1) Where does
game-theoretic reasoning happen?} Offline only (train a strategy, then just
play it), or also \emph{online} (re-solve the current situation at the table)?
\textbf{(2) What stores the strategy?} Tables over abstracted infosets, or
neural networks? The four systems in this volume occupy four distinct corners:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]}]
  % axes
  \draw[->, thick] (0,0) -- (12.6,0);
  \draw[->, thick] (0,0) -- (0,6.4);
  \node[font=\scriptsize\itshape] at (6.3,-0.95) {strategy storage};
  \node[font=\scriptsize\itshape, rotate=90] at (-1.05,3.2) {game-theoretic search at play time};
  \node[font=\scriptsize] at (2.4,-0.45) {tables on abstraction};
  \node[font=\scriptsize] at (10.0,-0.45) {neural networks};
  \node[font=\scriptsize, rotate=90] at (-0.45,1.2) {none};
  \node[font=\scriptsize, rotate=90] at (-0.45,5.2) {re-solve every spot};

  % systems as dots
  \tikzstyle{sys}=[circle, fill=accent, inner sep=2pt]
  \node[sys, label={[font=\scriptsize\bfseries]above:Pluribus (2019)},
        label={[font=\tiny, yshift=-1mm]below:blueprint + search, no nets}] at (2.6,5.0) {};
  \node[sys, fill=heroG, label={[font=\scriptsize\bfseries]above:ReBeL (2020)},
        label={[font=\tiny, yshift=-1mm]below:value net + CFR search}] at (8.2,5.4) {};
  \node[sys, fill=heroG,
        label={[font=\scriptsize, align=center]below:{\textbf{Player of Games (2021)}\\ \tiny sound search, any game}}] at (10.8,4.4) {};
  \node[sys, fill=memO, label={[font=\scriptsize\bfseries]above:Deep CFR (2019)},
        label={[font=\tiny, yshift=-1mm]below:this project}] at (8.6,1.4) {};
  \node[sys, fill=villR, label={[font=\scriptsize\bfseries]above:AlphaHoldem (2022)},
        label={[font=\tiny, yshift=-1mm]below:pure RL, no game theory}] at (11.2,1.0) {};
\end{tikzpicture}
\end{center}

\begin{intuition}
Read the map as a tug-of-war between two resources: \textbf{offline training
compute} (bottom row pays it all upfront, then plays instantly) and
\textbf{online thinking time} (top row keeps solving at the table, which buys
precision but costs seconds and engineering). The diagonal from Pluribus to
AlphaHoldem is also a diagonal of \emph{decreasing game theory and increasing
deep learning}.
\end{intuition}

\subsection{Refresher: the CFR core that three of the four systems share}

Pluribus fills its blueprint with it, ReBeL runs it inside every subgame,
Player of Games runs its regret-update phase with it --- only AlphaHoldem
drops it. So here is tabular CFR once, compactly, with its two classic
upgrades shown inline (the full pedagogical build-up lives in the companion
volume, Sections 2--3):

\begin{lstlisting}[style=py, caption={CFR in one block. \texttt{reach[p]} is the probability that player $p$'s own choices lead here; regrets are weighted by the \emph{opponent's} reach, the average strategy by \emph{your own}. The commented lines show the vanilla / CFR$^{+}$ variants of the update; the active lines are Linear CFR (weight everything by $t$), which is what Pluribus and Deep CFR use.}]
R, S = tables_of_zeros(), tables_of_zeros()  # regrets / average strategy

def cfr(state, t, reach):
    if state.is_terminal():
        return state.payoffs()                    # (u_0, u_1), zero-sum
    I, p  = infoset(state), state.to_act()
    sigma = regret_matching(R[I])                 # positive part, normalized
    v, v_I = {}, [0.0, 0.0]
    for a in state.legal():
        r = list(reach)
        r[p] *= sigma[a]                          # my move scales MY reach
        v[a] = cfr(state.child(a), t, r)
        v_I  = [x + sigma[a] * y for x, y in zip(v_I, v[a])]
    for a in state.legal():
        # vanilla CFR:  R[I][a] +=     reach[1-p] * (v[a][p] - v_I[p])
        # CFR+:         R[I][a]  = max(R[I][a] + reach[1-p] * (..), 0)
        R[I][a] += t * reach[1 - p] * (v[a][p] - v_I[p])   # Linear CFR
    S[I] += t * reach[p] * sigma          # the average strategy = the output
    return v_I

def solve(T):
    for t in range(1, T + 1):
        cfr(deal_random_hand(), t, reach=[1.0, 1.0])   # chance sampled
    return normalize(S)                   # converges to Nash (2 players)
\end{lstlisting}

% ════════════════════════════════════════════════════════════════════════════
\section{Pluribus: superhuman six-player poker, no neural network}
% ════════════════════════════════════════════════════════════════════════════

\textbf{Paper:} Brown \& Sandholm, \emph{Superhuman AI for multiplayer poker},
Science 2019. \quad
\textbf{One idea:} a coarse offline ``blueprint'' strategy plus cheap
\emph{depth-limited} online re-solving is enough --- even with six players,
even with tables instead of networks.

\subsection{Why six players changes the rules of the game}

Everything in the Deep CFR tutorial relied on the two-player zero-sum
correspondence: regret minimization $\Rightarrow$ Nash, and Nash
$\Rightarrow$ unbeatable. With six players \emph{both} halves break:

\begin{defbox}
In games with $3+$ players, computing a Nash equilibrium is computationally
intractable, and --- worse --- \emph{playing} one is no longer a guarantee:
if the others deviate, your equilibrium share can lose. Pluribus therefore
drops the theoretical target entirely and aims for an \emph{empirical} one:
a strategy that consistently beats elite humans. CFR-style self-play is kept
as a \emph{heuristic} that happens to produce strong, balanced play.
\end{defbox}

\subsection{Stage 1 (offline): the blueprint}

The blueprint is classic pre-deep-learning machinery, exactly what Deep CFR
was designed to replace --- and Pluribus shows it is still viable when paired
with search:

\begin{itemize}[leftmargin=1.4em, itemsep=2pt]
\item \textbf{Information abstraction:} strategically similar hands are
  bucketed (k-means on equity distributions), shrinking $10^{161}$ situations
  to a tractable table.
\item \textbf{Action abstraction:} only a handful of bet sizes exist in the
  abstract game.
\item \textbf{Linear MCCFR} (the same external-sampling traversals as our
  trainer, plus the same linear $t$-weighting) fills the regret tables ---
  this is exactly the Linear-CFR update of Listing~1, run with sampling.
\end{itemize}

The famous headline: the blueprint trained in \textbf{8 days on a 64-core
server}, under 512\,GB RAM, \textbf{no GPU} --- roughly \$150 of cloud compute,
versus millions of dollars of TPU time for Go-class systems.

\subsection{Stage 2 (online): re-solving --- but what exactly do you re-solve?}

The blueprint alone is too coarse to beat pros (the abstraction blurs exact
bet sizes and hand distinctions), so at the table Pluribus computes a fresh,
finer strategy for the situation it is actually in. In chess that sentence is
easy to make precise: \emph{search from the current position}. In poker the
analogous sentence does not even type-check, and seeing why is most of
understanding online re-solving.

\textbf{Problem 1: the ``position'' is not a state.} What the table shows
(board, pot, betting history) is only the \emph{public state}; its value
depends on which hands the players might be holding. So the root of a poker
subgame must carry, for every player, a \emph{range}: a probability
distribution over all 1326 hole-card pairs. And ranges do not fall from the
sky --- they come from strategies. \emph{If} you assume each player has been
following a known strategy so far (the blueprint, plus the outputs of any
earlier re-solves), then every public action they took is evidence about
their hand, and Bayes' rule turns the betting history into a range:
hands that would have folded to the flop raise get probability ${\sim}0$,
hands that would have 3-bet get boosted, and so on. Pluribus knows its
\emph{own} strategy exactly, so its own range is exact; for opponents it
assumes they played approximately the blueprint.

\textbf{Problem 2: the subgame still reaches too deep.} Even rooted on the
flop, a no-limit subgame spans turn and river --- far too large to solve in
seconds. Truncating it at a depth limit needs care, and that is the paper's
key invention.

So one online solve looks like this end to end:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  pstep/.style={rectangle, rounded corners=1.5mm, draw=accent, fill=blue!5, thick,
               align=center, minimum height=15mm, minimum width=33mm, font=\scriptsize}]
  \node[pstep] (s1) at (0,0)
    {\textbf{1. Bayes the history}\\ betting line $\to$ a range\\ for every player\\ (assuming blueprint play)};
  \node[pstep] (s2) at (4.35,0)
    {\textbf{2. build the subgame}\\ public state + ranges,\\ exact bet sizes faced,\\ depth-limited};
  \node[pstep] (s3) at (8.7,0)
    {\textbf{3. solve it}\\ Linear CFR over the\\ \emph{whole} range (all 1326\\ hands at once)};
  \node[pstep] (s4) at (13.05,0)
    {\textbf{4. only now look}\\ \textbf{at your cards}\\ play the action of the\\ row for your real hand};
  \draw[->, very thick] (s1) -- (s2);
  \draw[->, very thick] (s2) -- (s3);
  \draw[->, very thick] (s3) -- (s4);
\end{tikzpicture}
\end{center}

\subsection{Trick 1: depth limits with continuation strategies}

Cutting the subgame off at a depth limit means writing a \emph{number} at
each cut point: ``the value of arriving here''. Any \emph{single} assumption
about how play continues below the cut (e.g.\ ``everyone plays the blueprint
afterwards'') is an assumption the search above will learn to exploit. The
fix:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  nd/.style={circle, draw=accent, fill=blue!8, thick, minimum size=6mm, inner sep=1pt},
  leafnd/.style={rectangle, draw=memO, fill=orange!10, thick, minimum size=6mm, inner sep=2pt},
  cont/.style={rectangle, rounded corners=1mm, draw=black!55, fill=black!4,
               font=\scriptsize, inner sep=2.5pt}]
  % subgame triangle
  \node[nd] (root) at (0,4.2) {};
  \node[font=\scriptsize, anchor=east] at (-0.4,4.2) {current public state};
  \node[nd] (a) at (-1.6,3.0) {};
  \node[nd] (b) at ( 1.6,3.0) {};
  \draw[->] (root) -- (a); \draw[->] (root) -- (b);
  \node[leafnd] (l1) at (-2.6,1.8) {};
  \node[leafnd] (l2) at (-0.6,1.8) {};
  \node[leafnd] (l3) at ( 0.6,1.8) {};
  \node[leafnd] (l4) at ( 2.6,1.8) {};
  \draw[->] (a) -- (l1); \draw[->] (a) -- (l2);
  \draw[->] (b) -- (l3); \draw[->] (b) -- (l4);
  \node[font=\scriptsize\itshape, color=memO, anchor=west] at (3.5,1.8)
    {depth-limited leaves (not the end of the hand!)};

  % continuation fan under one leaf
  \node[cont] (c1) at (-5.0,0.3) {blueprint};
  \node[cont] (c2) at (-3.0,0.3) {fold-biased};
  \node[cont] (c3) at (-0.9,0.3) {call-biased};
  \node[cont] (c4) at ( 1.3,0.3) {raise-biased};
  \draw[->, black!60] (l1) -- (c1);
  \draw[->, black!60] (l1) -- (c2);
  \draw[->, black!60] (l1) -- (c3);
  \draw[->, black!60] (l1) -- (c4);
  \node[font=\scriptsize, align=left, anchor=west] at (3.0,0.3)
    {at each leaf, \emph{every} player picks one of\\
     \textbf{4 continuation strategies} for the rest of the hand};
\end{tikzpicture}
\end{center}

\begin{intuition}
\textbf{Why continuation strategies make shallow leaves honest.} If you
evaluate a leaf with a \emph{single} fixed policy (say, the blueprint), the
search can ``cheat'': it steers play toward leaves where the blueprint plays
badly for the opponent, producing a strategy that only works if the opponent
cooperates. Letting every player \emph{choose among several continuations} at
the leaf is a miniature game: the value of the leaf is what you can get when
the opponent picks their best continuation \emph{against you}. The search can
no longer assume a docile opponent --- at a tiny fraction of the cost of
solving to the end of the hand.
\end{intuition}

The three biased continuations are cheap to build --- take the blueprint,
multiply the probability of the biased action (fold / call / raise) by a
constant, renormalize --- and the value of a leaf under any combination of
choices is estimated by rolling the hand out under those strategies. The
choices themselves are folded into the CFR solve: each player's leaf-choice
is just one more decision in the subgame, so the equilibrium the solver finds
is already robust against all $4^{\text{players}}$ ways play could continue.

\subsection{Trick 2: solve for your whole range, not for your hand}

A chess engine searches for the best move \emph{in the position it sees}.
The poker analogue --- ``find the best line for the two kings I am
holding'' --- would be a disaster, and understanding why is the other half of
online re-solving. Your opponents never observe your hand; they observe your
\emph{actions}. If the strategy you compute is a function of your actual
hand, your actions become a code for it, and an attentive opponent reads the
code: you bet only your strong hands, they fold exactly then, your strong
hands stop earning. The whole point of a balanced (GTO) strategy is that the
\emph{same} observable action is taken by strong and weak hands in calibrated
proportions.

So step 3 of the pipeline solves the subgame \textbf{for every hand Pluribus
could be holding at once}: the output $\sigma$ is a whole table --- one row
per hole-card pair in its range, one action distribution per row --- computed
jointly so that the \emph{range as a whole} is unexploitable. Only after the
solve does Pluribus look at its actual cards and read off that single row
(step 4). Bluffs are not a separate mechanism: the equilibrium simply
assigns some raising probability to weak rows so that strong rows get paid
--- balance is an \emph{output} of range-level solving, not a style knob.

\begin{intuition}
This also explains an implementation detail: ``vector-based'' Linear CFR.
Since the subgame must be solved for all 1326 hands anyway, the efficient
form sweeps the public tree once per iteration carrying a 1326-long
\emph{vector} of regrets/values per node, instead of traversing once per
hand. Small subgames use this exact vector form; very large ones fall back
to sampling (Monte Carlo Linear CFR).
\end{intuition}

\subsection{The full play-time loop}

Putting both tricks into the pipeline, here is what happens at a Pluribus
decision on, say, the flop:

\begin{enumerate}[leftmargin=1.6em, itemsep=1pt]
\item \textbf{Freeze the public state}: board, pot, the exact bet it faces
  (no rounding --- the subgame is built with the true size).
\item \textbf{Compute ranges} for all players by Bayes from the betting
  history, assuming blueprint play (its own range: exact).
\item \textbf{Build the depth-limited subgame} from this root --- typically
  the rest of the current street plus the start of the next --- and attach
  the four continuation choices at every leaf (Trick 1).
\item \textbf{Solve with Linear CFR for the whole range} (Trick 2), a few
  seconds on two CPUs.
\item \textbf{Play}: sample an action from the solved row of its actual
  hand.
\item \textbf{If an opponent later uses a bet size outside the current
  tree}: build a \emph{new} subgame that contains that exact size and
  re-solve --- \emph{nested} re-solving, repeated as often as needed. (This
  is how off-tree actions are handled exactly instead of being rounded to
  the abstraction, the classic leak of blueprint-only bots.)
\end{enumerate}

\begin{lstlisting}[style=py, caption={Pluribus, schematically. The blueprint is Deep CFR's ancestor (same traversals, tables instead of nets); play time adds range-aware, depth-limited re-solving.}]
def blueprint(T):                      # OFFLINE, once, ~8 days on CPUs
    for t in range(1, T + 1):
        for p in range(6):             # six players!
            mccfr_traverse(deal(), p, t)   # external sampling, linear weights
    return regret_tables               # indexed by ABSTRACT infosets

def resolve(public_state, ranges):     # ONLINE: one subgame solve
    G = subgame(public_state, ranges,            # root carries the RANGES
                depth_limit=rest_of_street_plus,
                bet_sizes=exact_bets_faced)      # no rounding
    for leaf in G.leaves():            # Trick 1: 4-way continuation choice
        leaf.attach_choices([blueprint, fold_bias, call_bias, raise_bias])
    return linear_cfr(G)               # Trick 2: sigma[hand] for ALL hands

def act(history, board, my_hand):
    if preflop and on_tree(history):   # preflop abstraction is dense:
        return sample(blueprint[abstract(history, my_hand)])
    ranges = bayes_ranges(history, board, assumed=blueprint)
    sigma  = resolve(public_state(history, board), ranges)
    return sample(sigma[my_hand])      # look at MY cards only at the end
# off-tree opponent bet later? build a new subgame with that exact size
# and call resolve() again (nested re-solving).
\end{lstlisting}

\subsection{Close-up: the two questions everyone asks}

\textbf{Q1 --- an opponent bets a size that is not in the tree. What happens,
mechanically?} Suppose the subgame Pluribus just solved offered the opponent
three sizes at some node --- $\{0.5\,\text{pot},\, 1\,\text{pot},\,
\text{all-in}\}$ --- and the human bets $0.37$ pot. The solution Pluribus is
holding has \emph{no entry} for what just happened. The historical fix
(``action translation'': round $0.37$ to the nearest size and pretend) is
exactly the leak humans learned to attack by betting \emph{between} the
abstraction points. Pluribus instead rebuilds:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  onode/.style={circle, draw=villR, fill=villR!10, thick, minimum size=7mm, inner sep=1pt},
  tnode/.style={rectangle, rounded corners=1mm, draw=black!55, fill=black!4,
                font=\scriptsize, inner sep=2.5pt},
  newb/.style={rectangle, rounded corners=1mm, draw=heroG, fill=heroG!12, very thick,
               font=\scriptsize, inner sep=2.5pt}]
  % BEFORE
  \node[font=\scriptsize\bfseries] at (0,2.6) {tree just solved};
  \node[onode] (o1) at (0,1.8) {O};
  \node[tnode] (b1) at (-1.7,0.5) {0.5 pot};
  \node[tnode] (b2) at (0,0.5) {1 pot};
  \node[tnode] (b3) at (1.7,0.5) {all-in};
  \draw[->] (o1) -- (b1); \draw[->] (o1) -- (b2); \draw[->] (o1) -- (b3);
  \node[font=\scriptsize, color=villR] at (0,-0.4)
      {human bets \textbf{0.37 pot}: no branch!};

  \draw[->, very thick] (3.3,1.0) -- node[above, font=\scriptsize, align=center]
      {rebuild +\\ re-solve} (5.2,1.0);

  % AFTER
  \node[font=\scriptsize\bfseries] at (9.2,2.6) {new (augmented) subgame};
  \node[onode] (o2) at (9.2,1.8) {O};
  \node[tnode] (c1) at (7.0,0.5) {0.5 pot};
  \node[tnode] (c2) at (8.5,0.5) {1 pot};
  \node[newb] (c3) at (10.0,0.5) {0.37 pot};
  \node[tnode] (c4) at (11.6,0.5) {all-in};
  \draw[->] (o2) -- (c1); \draw[->] (o2) -- (c2);
  \draw[->, heroG, very thick] (o2) -- (c3); \draw[->] (o2) -- (c4);
  \node[font=\scriptsize, color=heroG!60!black, align=center] at (9.2,-0.55)
      {the new equilibrium decides \emph{which hands} bet 0.37 pot\\
       $\to$ posterior range comes from the solve itself, no rounding};
\end{tikzpicture}
\end{center}

\begin{enumerate}[leftmargin=1.6em, itemsep=1pt]
\item \textbf{Rebuild}: a fresh subgame rooted just before the bet, with the
  opponent's action set at that node \emph{augmented} by the exact
  $0.37$-pot size.
\item \textbf{Ranges at the new root} are taken from \emph{before} the
  off-tree action --- everything up to that moment was on-model, so Bayes is
  still well-defined.
\item \textbf{Re-solve} the augmented subgame as usual (depth limit,
  continuation leaves, whole-range Linear CFR).
\item The subtle part: the new equilibrium contains \textbf{a strategy for
  the opponent at the augmented node} --- it computes \emph{which hands
  would bet $0.37$ pot at equilibrium}. Bayes against \emph{that} computed
  strategy gives the range going forward. The off-tree action is never
  mapped to a fiction; the solver \emph{invents its correct interpretation}
  as part of the equilibrium.
\item Another off-tree size on a later street? Do it all again --- hence
  \emph{nested} re-solving.
\end{enumerate}

\begin{intuition}
The asymmetry to remember: Pluribus's \emph{own} bet sizes can stay few ---
it chooses them. It is the \emph{opponent's} freedom that makes the full
game impossible to pre-solve. Nested re-solving prices each opponent choice
\emph{at the moment it happens} instead of pre-computing responses to a
continuum --- the same ``pay compute only for situations that actually
arise'' logic that motivates search in the first place. (Preflop, where
Pluribus plays the blueprint directly, the action abstraction is dense
enough that rounding a rare off-tree size costs little.)
\end{intuition}

\textbf{Q2 --- the ranges assume opponents follow the blueprint. What if
they just\dots\ don't?} Separate three cases.

\emph{Case 1: the deviation is an off-tree action.} That is Q1 ---
structural, fully handled; the augmented equilibrium even defines what the
new action ``means''.

\emph{Case 2: on-tree actions, wrong frequencies.} The human raises twice as
often as the blueprint would. Pluribus's posterior range for them is then
simply \emph{wrong} --- it credits the line with blueprint frequencies. And
Pluribus does \emph{nothing} about it: by deliberate design it has no
opponent modeling and never adapts to observed tendencies. Partly
robustness (an adapting bot can be manipulated by feigned tendencies for
later betrayal), partly principle: its defense is \textbf{balance, not
mind-reading}. A range is a statement about a \emph{model}, not about the
human --- Bayes answers ``which hands would have taken this line \emph{if}
the opponent played the assumed strategy''.

\emph{Case 3: so why is playing on-model still sound?} Zero-sum equilibrium
logic: the strategy Pluribus computes is (approximately) unexploitable
\emph{within the model game}, and an opponent playing off-model frequencies
is, from the model's viewpoint, deviating from equilibrium --- deviations do
not gain in expectation. The model's mixed strategies also keep ranges from
ever collapsing to zero on the human's true holding (regret matching leaves
small probability on many actions), and every nested re-solve refreshes the
range model. The honest catch is the next box.

\begin{trap}
\textbf{Where the soundness leak is.} An opponent can deviate \emph{early},
accept a small on-model loss, \emph{poison the assumed range}, and harvest a
larger error from the re-solver later --- this is exactly what makes naive
(``unsafe'') re-solving theoretically attackable in two-player zero-sum.
Libratus closed the hole with \emph{safe} re-solving gadgets that guarantee
the re-solved strategy never does worse than the blueprint against any
opponent hand. Pluribus --- playing six-max, where equilibrium guarantees
were abandoned anyway (Sec.\ 2.1) --- accepts the heuristic; empirically,
five elite professionals over 10\,000 hands did not find a way in. One more
instance of the multiplayer trade: theory exchanged for scale, validated at
the table.
\end{trap}

\textbf{Results.} Against elite professionals (including World Series
champions), in both 5 humans $+$ 1 AI and 1 human $+$ 5 copies formats,
Pluribus won decisively. Each decision takes seconds on two CPUs.

\begin{trap}
Pluribus is often summarized as ``deep learning beats poker pros''. It
contains \textbf{no neural network at all}. Its lesson is the opposite of the
hype: \emph{search and good abstractions are devastatingly cost-effective}.
When you read a new-method paper, always ask what a tuned
blueprint-plus-search baseline would have done.
\end{trap}

% ════════════════════════════════════════════════════════════════════════════
\section{ReBeL: reinforcement learning + search on belief states}
% ════════════════════════════════════════════════════════════════════════════

\textbf{Paper:} Brown, Bakhtin, Lerer, Gong, \emph{Combining Deep Reinforcement
Learning and Search for Imperfect-Information Games}, NeurIPS 2020. \quad
\textbf{One idea:} an imperfect-information game becomes a
\emph{perfect-information} game if the ``state'' is taken to be the
\textbf{public belief state} --- then AlphaZero-style self-play $+$ search
applies, with CFR doing the searching.

\subsection{The conversion trick}

\begin{defbox}
\textbf{Public belief state (PBS).} Everything publicly visible (board,
pot, betting history) \emph{plus}, for each player, the probability
distribution over their possible private hands, as computable by an outside
observer who knows both players' strategies. In hold'em each belief is a
vector over the 1326 possible hole-card pairs.
\end{defbox}

Why this works: if both players' policies are common knowledge, then Bayes'
rule updates the beliefs after every public action --- so the PBS evolves
\emph{deterministically given the policies}, like a board position. Hidden
information has been traded for a bigger, continuous state. Values become
well-defined functions of the PBS, so a neural network $V(\mathrm{PBS})$ can
be trained the way AlphaZero trains its value net.

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  pbs/.style={ellipse, draw=heroG, fill=heroG!8, thick, align=center,
              minimum height=10mm, inner sep=3pt},
  sub/.style={isosceles triangle, isosceles triangle apex angle=58, shape border rotate=-90,
              draw=accent, fill=blue!6, thick, minimum height=16mm, inner sep=1pt},
  net/.style={rectangle, rounded corners=1.5mm, draw=villR, fill=red!5, thick,
              align=center, inner sep=3pt}]
  \node[pbs] (b0) at (0,0) {PBS $\beta_t$\\ \scriptsize board, pot,\\ \scriptsize beliefs};
  \node[sub] (g)  at (4.0,-0.1) {};
  \node[font=\scriptsize, align=center, fill=white, inner sep=1pt] at (4.0,1.15)
      {depth-limited subgame};
  \node[net] (v) at (4.0,-2.6) {$V(\mathrm{PBS};\theta_v)$\\ \scriptsize value net at the leaves};
  \node[pbs] (b1) at (8.6,0) {PBS $\beta_{t+1}$\\ \scriptsize beliefs updated\\ \scriptsize by Bayes};

  \draw[->, thick] (b0) -- node[above, font=\scriptsize]{solve with CFR} (g);
  \draw[->, thick, villR] (g.south) -- node[right, font=\scriptsize]{leaf values} (v);
  \draw[->, thick] (g.east) -- node[above, font=\scriptsize, align=center]
      {play action,\\ update beliefs} (b1);
  \draw[->, thick, dashed, memO] (g.north) .. controls (4.0,2.0) and (0.5,2.0) ..
      node[above, font=\scriptsize, color=memO]{subgame values $\to$ training targets for $\theta_v$} (b0.north);
\end{tikzpicture}
\end{center}

\subsection{The self-play loop, one hand at a time}

The loop is easiest to follow as a single hand of self-play, narrated.
First, the cast: ReBeL plays \emph{both} seats. At the start of an episode a
real deal hands private cards to each seat (the \emph{world state}); on top
of that, the algorithm maintains one shared bookkeeping object, the PBS ---
which contains no secrets (board, pot, two belief vectors), so it looks
identical from both seats. The hand then alternates between \emph{solving}
(a fresh subgame at the current PBS) and \emph{stepping} (an action is
played, beliefs update, possibly new board cards arrive):

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  pbs/.style={ellipse, draw=heroG, fill=heroG!8, thick, align=center,
              inner sep=2pt, font=\scriptsize},
  buf/.style={rectangle, rounded corners=1.5mm, draw=memO, fill=orange!8, thick,
              align=center, font=\scriptsize, minimum height=8mm},
  net/.style={rectangle, rounded corners=1.5mm, draw=accent, fill=blue!6, thick,
              align=center, font=\scriptsize, minimum height=8mm}]
  \node[pbs] (b0) at (0,0)    {$\beta_0$\\ preflop};
  \node[pbs] (b1) at (3.6,0)  {$\beta_1$};
  \node[pbs] (b2) at (7.2,0)  {$\beta_2$};
  \node[font=\small] (dots) at (9.6,0) {$\cdots$};
  \node[pbs] (bT) at (11.6,0) {terminal\\ (payoffs)};
  \draw[->, thick] (b0) -- node[above, font=\tiny, align=center]
      {solve $G_0$,\\ act, Bayes} (b1);
  \draw[->, thick] (b1) -- node[above, font=\tiny, align=center]
      {solve $G_1$,\\ act, Bayes} (b2);
  \draw[->, thick] (b2) -- (dots);
  \draw[->, thick] (dots) -- (bT);

  \node[buf, minimum width=86mm, align=center] (buffer) at (4.4,-2.2)
      {training buffers --- each solve feeds BOTH:\\
       value target $(\beta_t,\; v_t)$ \qquad policy target $(\beta_t,\; \bar\sigma_t)$};
  \draw[->, dashed, memO] (b0.south) -- (buffer.north -| b0.south);
  \draw[->, dashed, memO] (b1.south) -- (buffer.north -| b1.south);
  \draw[->, dashed, memO] (b2.south) -- (buffer.north -| b2.south);
  \node[net] (vnet) at (11.4,-1.5) {refit $V(\cdot\,;\theta_v)$:\\ value regression};
  \node[net] (pinet) at (11.4,-3.0) {refit $\pi(\cdot\,;\theta_\pi)$:\\ policy distillation\\ $\to$ next warm starts};
  \draw[->, thick, memO] (buffer.east) -- (vnet.west);
  \draw[->, thick, memO] (buffer.east) -- (pinet.west);
\end{tikzpicture}
\end{center}

At each decision point, six things happen, in order:

\begin{enumerate}[leftmargin=1.6em, itemsep=1pt]
\item \textbf{Freeze the current PBS} $\beta_t$: board, pot, and both belief
  vectors as they stand.
\item \textbf{Build the depth-limited subgame} $G_t$ rooted at $\beta_t$ ---
  roughly the rest of the current betting round, stopping wherever new cards
  would be dealt. Building fixes the \emph{shape only}: no value is attached
  to any leaf yet, because a leaf's value depends on the beliefs \emph{at
  that leaf}, and those do not exist until a strategy says how players reach
  it --- which is exactly what the next step is about to compute.
\item \textbf{Solve it}: $T_{\mathrm{cfr}}$ iterations of plain
  \emph{tabular} CFR inside $G_t$ (volume 1's algorithm, literally), with
  leaf evaluation happening \emph{inside every iteration}: the current
  iterate's $\sigma$ induces beliefs at each leaf (Bayes along the path),
  the net is queried at that freshly built leaf PBS, and the returned
  per-hand values feed the regret updates. Strategy and leaf values
  \emph{co-evolve} --- every iteration re-prices the leaves under the new
  beliefs. The policy net $\theta_\pi$ warm-starts the iterations so fewer
  are needed.
\item \textbf{Store two training pairs} --- one solve feeds both networks.
  Let $\bar\sigma_t$ be the \emph{average} of the $T_{\mathrm{cfr}}$
  policies (the object volume 1 proved converges). For the \textbf{value
  net}: $(\beta_t,\, v_t)$, where $v_t$ is the per-hand value vector of the
  root under $\bar\sigma_t$ --- ``what this PBS is worth under
  near-equilibrium play'', AlphaZero-style value labels. For the
  \textbf{policy net}: $(\beta_t,\, \bar\sigma_t)$ itself, the root's
  per-hand action distributions --- distilled later by $\theta_\pi$, these
  become tomorrow's warm starts.
\item \textbf{Choose what to actually play}: pick \emph{one random
  iteration} $t' \sim \mathrm{uniform}(1..T_{\mathrm{cfr}})$ and use its
  policy $\sigma_{t'}$ --- \emph{not} the average. This is the
  strangest-looking choice in the method; both reasons (training-distribution
  consistency and safety-by-randomization) are unpacked in the close-up,
  Q2 below.
\item \textbf{Act and update}: the seat to act looks at its real cards and
  samples its row of $\sigma_{t'}$; the action is public; Bayes' rule pushes
  \emph{both} belief vectors through $\sigma_{t'}$, giving $\beta_{t+1}$
  (if a street ends, the dealt board cards update the PBS too).
\end{enumerate}

When the hand reaches a terminal PBS, the episode's pairs join the two
buffers; after many hands, $\theta_v$ is refit on the value buffer
(regression) and $\theta_\pi$ is distilled from the policy buffer ---
closing the loop: \emph{today's average policies become tomorrow's warm
starts}. \textbf{At test time against a human, the identical loop runs
minus the stores} --- search-with-net-leaves \emph{is} the player; training
merely makes the nets worth searching with.

\begin{lstlisting}[style=py, caption={ReBeL's training loop --- the six steps above, in code. The same machinery runs at test time, minus the buffer writes.}]
def rebel_episode(theta_v, theta_pi, v_buffer, pi_buffer):
    deal = sample_world()                    # real cards for both seats
    beta = initial_pbs()                     # (1) blinds, uniform beliefs
    while not beta.is_terminal():
        G = depth_limited_subgame(beta)      # (2) SHAPE only, no values yet
        policies = run_cfr(G, T_cfr,         # (3) tabular CFR; EVERY iter:
                           warm_start=theta_pi)  # leaf beliefs from current
                                             # sigma -> query V(.; theta_v)
        sigma_avg = average(policies)        # the converging object (vol. 1)
        v_buffer.add(beta,                   # (4a) value target: root values
                     value_of_root(G, sigma_avg))      # under the average
        pi_buffer.add(beta,                  # (4b) policy target: the root's
                      sigma_avg.at_root())   # per-hand action distributions
        sigma = policies[randint(1, T_cfr)]  # (5) play a RANDOM iterate
                                             #     (why: close-up, Q2)
        hand = deal[beta.to_act()]
        a = sample(sigma[hand])              # (6) act with the real cards...
        beta = bayes_update(beta, sigma, a)  #     ...Bayes BOTH beliefs
    refit(theta_v, v_buffer)                 # after many hands: regression
    refit(theta_pi, pi_buffer)               # + distillation -> warm starts
\end{lstlisting}

\subsection{One solve under the microscope: a worked miniature}

Steps 2--3 said ``build the subgame, solve it with CFR''. To actually
\emph{see} that, shrink poker until the whole subgame fits in one picture:
each player holds one of \textbf{three hand types} --- $A$ (the nuts), $B$
(medium), $C$ (air) --- instead of 1326; the board is a flop, the pot is 6
chips, there is a single bet size (pot), and the depth limit is the end of
this street. Both belief vectors at the root are uniform
$(\frac13,\frac13,\frac13)$. Here is the \emph{entire} subgame $G_t$ that
step 2 builds:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]},
  p1/.style={rectangle, draw=heroG, fill=heroG!12, thick, minimum size=6.5mm, inner sep=1pt},
  p2/.style={circle, draw=villR, fill=villR!12, thick, minimum size=6.5mm, inner sep=1pt},
  term/.style={rectangle, rounded corners=1mm, draw=black!50, fill=black!8,
               font=\scriptsize, align=center, inner sep=2.5pt},
  dleaf/.style={rectangle, rounded corners=1mm, draw=memO, fill=orange!15, thick,
               font=\scriptsize, align=center, inner sep=2.5pt},
  elab/.style={font=\scriptsize, fill=white, inner sep=1pt}]

  % root belief bars (two groups, top-left)
  \node[font=\tiny] at (0.55,6.45) {P1 range};
  \draw[black!50] (0,5.6) -- (1.1,5.6);
  \fill[heroG!70] (0.05,5.6) rectangle +(0.25,0.40);
  \fill[heroG!70] (0.40,5.6) rectangle +(0.25,0.40);
  \fill[heroG!70] (0.75,5.6) rectangle +(0.25,0.40);
  \node[font=\tiny] at (0.17,5.45) {A}; \node[font=\tiny] at (0.52,5.45) {B};
  \node[font=\tiny] at (0.87,5.45) {C};
  \node[font=\tiny] at (2.25,6.45) {P2 range};
  \draw[black!50] (1.7,5.6) -- (2.8,5.6);
  \fill[villR!70] (1.75,5.6) rectangle +(0.25,0.40);
  \fill[villR!70] (2.10,5.6) rectangle +(0.25,0.40);
  \fill[villR!70] (2.45,5.6) rectangle +(0.25,0.40);
  \node[font=\tiny] at (1.87,5.45) {A}; \node[font=\tiny] at (2.22,5.45) {B};
  \node[font=\tiny] at (2.57,5.45) {C};
  \node[font=\tiny, align=center] at (1.4,5.0) {root beliefs:\\ uniform};

  % the tree
  \node[p1] (root) at (6.0,6.0) {P1};
  \node[font=\scriptsize, anchor=west] at (6.5,6.3) {pot 6, P1 to act};

  \node[p2] (p2c) at (3.4,4.4) {P2};
  \node[p2] (p2b) at (9.0,4.4) {P2};
  \draw[->] (root) -- node[elab, above left=0pt and 2pt]{check} (p2c);
  \draw[->] (root) -- node[elab, above right=0pt and 2pt]{bet 6} (p2b);

  \node[dleaf] (L1) at (1.3,2.6) {street over\\ pot 6\\ $\Rightarrow$ ask the net};
  \node[p1]   (p1f) at (5.0,2.6) {P1};
  \draw[->] (p2c) -- node[elab, left=2pt]{check} (L1);
  \draw[->] (p2c) -- node[elab, right=2pt]{bet 6} (p1f);

  \node[term] (T1) at (3.6,0.7) {P2 wins 6\\ (arithmetic)};
  \node[dleaf] (L2) at (6.4,0.7) {street over\\ pot 18\\ $\Rightarrow$ ask the net};
  \draw[->] (p1f) -- node[elab, left=2pt]{fold} (T1);
  \draw[->] (p1f) -- node[elab, right=2pt]{call} (L2);

  \node[term] (T2) at (7.9,2.6) {P1 wins 6\\ (arithmetic)};
  \node[dleaf] (L3) at (10.6,2.6) {street over\\ pot 18\\ $\Rightarrow$ ask the net};
  \draw[->] (p2b) -- node[elab, left=2pt]{fold} (T2);
  \draw[->] (p2b) -- node[elab, right=2pt]{call} (L3);

  % legend
  \node[term, anchor=west]  at (12.0,4.8) {fold terminal:\\ payoff known};
  \node[dleaf, anchor=west] at (12.0,3.4) {depth leaf:\\ a turn card\\ would come next\\ $\Rightarrow$ $V(\beta_{\mathrm{leaf}};\theta_v)$};
  \node[font=\scriptsize\itshape, align=left, anchor=west, text width=33mm] at (12.0,1.4)
    {beliefs at a leaf are\\ \emph{not} the root beliefs:\\ they were Bayes'd\\ through the path's\\ actions (see below)};
\end{tikzpicture}
\end{center}

Two kinds of leaves, and the distinction is the whole method. \textbf{Fold
terminals} (gray) need no intelligence: the pot goes to the survivor,
arithmetic. \textbf{Depth leaves} (orange) are where the street ends and a
turn card would be dealt --- everything below them was cut off, so their
value is a query to the value net, \emph{at the leaf's own PBS}: same board
and pot, but belief vectors that have been pushed through every action on
the path. Concretely, here is that Bayes push for the root action ``P1
bets'', with numbers --- suppose the current CFR iterate has P1 betting
$60\%$ of its $A$ hands, $10\%$ of $B$, $20\%$ of $C$ (a value-bet/bluff
mix):

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]}]
  % prior bars
  \node[font=\scriptsize\bfseries] at (1.0,2.45) {P1's range before};
  \draw[black!50] (0,0.8) -- (2.0,0.8);
  \fill[heroG!70] (0.15,0.8) rectangle +(0.40,0.53);
  \fill[heroG!70] (0.80,0.8) rectangle +(0.40,0.53);
  \fill[heroG!70] (1.45,0.8) rectangle +(0.40,0.53);
  \node[font=\tiny] at (0.35,1.50) {33\%}; \node[font=\tiny] at (1.00,1.50) {33\%};
  \node[font=\tiny] at (1.65,1.50) {33\%};
  \node[font=\tiny] at (0.35,0.62) {A}; \node[font=\tiny] at (1.00,0.62) {B};
  \node[font=\tiny] at (1.65,0.62) {C};

  % sigma box
  \node[rectangle, rounded corners=1.5mm, draw=accent, fill=blue!5, thick,
        align=center, font=\scriptsize] (sg) at (5.4,1.1)
    {current iterate $\sigma$:\\ P(bet $\mid A$) = 0.6\\ P(bet $\mid B$) = 0.1\\ P(bet $\mid C$) = 0.2};
  \draw[->, thick] (2.4,1.1) -- node[above, font=\scriptsize]{$\times$} (3.6,1.1);
  \draw[->, thick] (7.2,1.1) -- node[above, font=\scriptsize, align=center]
      {renormalize\\ $(6{:}1{:}2)/9$} (9.0,1.1);

  % posterior bars
  \node[font=\scriptsize\bfseries] at (10.6,2.45) {after ``P1 bets''};
  \draw[black!50] (9.5,0.8) -- (11.7,0.8);
  \fill[heroG!70] (9.65,0.8)  rectangle +(0.40,1.07);
  \fill[heroG!70] (10.30,0.8) rectangle +(0.40,0.18);
  \fill[heroG!70] (10.95,0.8) rectangle +(0.40,0.36);
  \node[font=\tiny] at (9.85,2.05)  {67\%}; \node[font=\tiny] at (10.50,1.16) {11\%};
  \node[font=\tiny] at (11.15,1.34) {22\%};
  \node[font=\tiny] at (9.85,0.62)  {A}; \node[font=\tiny] at (10.50,0.62) {B};
  \node[font=\tiny] at (11.15,0.62) {C};
\end{tikzpicture}
\end{center}

This one picture is three lines of the listing at once: it is the
\texttt{bayes\_update} that produces the next root PBS, it is what happens
\emph{inside} the tree between a node and its child, and it is why the
opponent must respect the bet --- the bettor's range really did get stronger
(67\% nuts), yet not pure (22\% air), because the \emph{solver} chose that
bluff ratio.

Now ``solve it'' (step 3) is no mystery: run volume 1's tabular CFR on this
little tree --- per-hand regret tables at the four decision nodes, sweeps
$t = 1..T_{\mathrm{cfr}}$ --- with one twist at the orange leaves:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  box/.style={rectangle, rounded corners=1.5mm, thick, align=center,
              font=\scriptsize, minimum height=13mm}]
  \node[box, draw=memO, fill=orange!10, minimum width=40mm] (leaf) at (0,0)
    {PBS at leaf L3:\\ board + pot 18\\ P1 beliefs (67,11,22)\%\\ P2 beliefs (..,..,..)\%};
  \node[box, draw=villR, fill=red!5, minimum width=22mm] (net) at (5.6,0)
    {value net\\ $V(\cdot\,;\theta_v)$};
  \node[box, draw=accent, fill=blue!5, minimum width=42mm] (out) at (11.2,0)
    {values \emph{per hand}:\\ P1: $A{:}{+}9$~~$B{:}{+}1$~~$C{:}{-}7$\\ P2: $A{:}{+}9$~~$B{:}{-}2$~~$C{:}{-}8$};
  \draw[->, very thick] (leaf) -- (net);
  \draw[->, very thick] (net) -- (out);
  \node[font=\scriptsize\itshape] at (5.6,-1.35)
    {(numbers illustrative; what matters is the \emph{shape}: beliefs in, one value per hand out)};
\end{tikzpicture}
\end{center}

And here is the subtle point that makes everything cohere: \textbf{the
beliefs at a leaf change as CFR iterates}. Iteration 5's $\sigma$ bets a
different mix than iteration 80's, so the \emph{same} leaf L3 is reached
with \emph{different} belief vectors, and the net must answer differently
--- a strong-range bet-call leaf is worth more to P1 than a bluff-heavy one.
That is exactly why $V$ takes the beliefs as input: one network must value
\emph{every possible range shape} at every public situation. Scaling back
up to real HUNL changes only sizes --- 3 hand types become 1326, one bet
size becomes a menu, one street becomes the rest of the round. Nothing
conceptual changes.

\begin{intuition}
\textbf{Where Deep CFR and ReBeL put the neural network.} Deep CFR's nets
\emph{replace the regret tables} inside one global solve. ReBeL's net replaces
\emph{the bottom of the search tree}: CFR itself stays tabular but only ever
sees a small window of the game, and the net summarizes everything below the
window. That is exactly AlphaZero's division of labor, transplanted to
imperfect information --- which is why the paper's title says ``RL $+$
Search''.
\end{intuition}

\subsection{Two phrases from the listing, unpacked}

\textbf{``Depth-limited subgame'' --- where exactly is the cut, and why
there?} Picture the \emph{full} game tree hanging below the current PBS: all
betting sequences of this street, then $\sim$47 possible turn cards
\emph{each} with all their betting sequences, then $\sim$46 rivers each with
theirs. The subgame keeps only the top slice --- every betting sequence of
the \emph{current} street --- and cuts exactly where the next card would be
dealt, because that chance node is where the tree multiplies by 47 and
becomes hopeless:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]}]
  % full game triangle
  \draw[thick, accent, fill=blue!5]
      (0,3.6) -- (-2.6,0.4) -- (2.6,0.4) -- cycle;
  \fill[black!12] (-1.72,-1.5) -- (-2.6,0.4) -- (2.6,0.4) -- (1.72,-1.5)
      -- cycle;
  \draw[black!40] (-1.72,-1.5) -- (-2.6,0.4)  (2.6,0.4) -- (1.72,-1.5);
  \node[circle, draw=heroG, fill=heroG!15, thick, inner sep=2pt] at (0,3.6) {};
  \node[font=\scriptsize, anchor=west] at (0.3,3.7) {root: current PBS};
  \node[font=\scriptsize, align=center] at (0,1.6)
      {solved exactly\\ (this street's betting,\\ the miniature above)};
  \draw[very thick, dashed, memO] (-3.4,0.4) -- (3.4,0.4)
      node[right, font=\scriptsize\bfseries, color=memO, align=left]
      {the cut: betting round over,\\ a turn card comes next};
  \node[font=\scriptsize, align=center, color=black!60] at (0,-0.7)
      {never built: 47 turn cards $\times$ betting $\times$ 46 rivers $\times$ \dots\\
       summarized by net calls $V(\beta_{\mathrm{leaf}};\theta_v)$ ---
       one per leaf \emph{per CFR iteration},\\
       because the leaf beliefs move with the evolving $\sigma$;\\
       each answer = expected value of all of it, per hand};
\end{tikzpicture}
\end{center}

A chess engine does the same thing when it searches 12 plies and then calls
its evaluation function instead of playing to checkmate. ReBeL's
``evaluation function'' just answers a harder question --- per hand and
belief-aware --- and is trained, not hand-written. The cut placement is the
\textbf{dial between compute and trust}: cut deeper and you do more exact
CFR work but lean on the net less; cut shallower and search is cheap but the
net's approximation error ($\varepsilon_{\mathrm{approx}}$, volume 3)
dominates. Cutting at the chance node is simply the sweet spot where each
net call replaces the largest possible amount of tree. (And to be precise
about \emph{when} those calls happen: not at build time --- a leaf has no
value until beliefs reach it. The leaves are priced inside the solve, anew
at every CFR iteration, as the previous section showed.)

\textbf{``warm\_start=theta\_pi'' --- what exactly is being warmed?}
Start from what CFR's internal state \emph{is}: nothing but the regret
tables $R[I]$ --- the strategy is always derived from them by regret
matching, $\sigma(a) \propto R^{+}(a)$. So ``starting CFR'' \emph{means}
choosing the initial contents of those tables. Cold start: all zeros
$\Rightarrow$ iteration 1 plays uniform $\Rightarrow$ the first iterations
are spent un-learning nonsense (volume 3 showed early iterates are garbage).

\begin{trap}
\textbf{Where does $\hat\sigma$ come from? Not from the value net --- and it
could not.} ReBeL trains \emph{two separate} networks. The value net
$\theta_v$ answers ``what is this leaf worth, per hand'' and serves the
depth-limit leaves. The policy net $\theta_\pi$ outputs \emph{action
probabilities directly}, per hand, and is trained by distillation: its
targets are the average strategies that past subgame solves produced (just
as AlphaZero's policy head distills MCTS visit counts). One might hope to
skip it and derive probabilities from values --- a softmax over action
values, say. Equilibrium play forbids this: \emph{at a mixed equilibrium,
every action in the mix has the same expected value} --- that is what being
willing to mix means. In a 70/30 bet/check spot, bet and check are worth
exactly the same \emph{to you}; the 70/30 is not there because betting is
``better'', it is what keeps the \emph{opponent} indifferent between her
responses. The mixing ratio is a property of the equilibrium, invisible in
your own values --- probabilities must be solved for or distilled, never
read off values.
\end{trap}

Now the trick, with numbers. Regret matching only looks at the
\emph{proportions} of the positive regrets. So if the policy net, shown this
subgame's PBS, predicts $\hat\sigma = (\text{bet } 70\%,\ \text{check }
30\%)$ at some infoset, then initializing that infoset's table to
\[
R_{\mathrm{init}} = w \cdot (0.7,\; 0.3)
\qquad\text{(any scale } w > 0\text{)}
\]
makes regret matching reproduce exactly $(70\%, 30\%)$ at iteration 1. Do
this at every infoset of the subgame, and CFR \emph{starts at the net's
guess} instead of at uniform.

The scale $w$ is not cosmetic --- it encodes \textbf{how much to trust the
guess}, in units of ``iterations' worth of evidence''. Too small and one
iteration of real regrets washes the guess out (nothing gained); too large
and CFR becomes too stubborn to repair the net's mistakes. Brown \&
Sandholm's strategy-based warm start is the principled recipe for choosing
$w$ so the tables look \emph{as if} the guess had been earned by genuinely
played iterations --- which is precisely why the convergence guarantee
survives.

And the guess \emph{is} repairable --- that is the whole point of still
running CFR. Suppose the net says ``bet 70\%'' but in \emph{this} exact
subgame (these ranges, these sizes) betting is actually mediocre. Iteration
1 plays 70/30; the traversal reveals check's values are higher; regret flows
onto check; within a few dozen iterations $\sigma$ has slid to, say, 40/60.
The net saved the long journey from uniform; CFR still has the last word:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]}]
  \draw[->, thick] (0,0) -- (8.8,0);
  \node[font=\scriptsize] at (7.6,-0.35) {CFR iterations};
  \draw[->, thick] (0,0) -- (0,3.2);
  \node[rotate=90, font=\scriptsize] at (-0.45,1.6) {exploitability};
  % "good enough" line
  \draw[dashed, black!50] (0,0.55) -- (8.5,0.55);
  \node[font=\tiny, color=black!60, anchor=south east] at (8.5,0.58)
      {good enough to play};
  % cold curve
  \draw[very thick, villR] plot[smooth] coordinates
      {(0.0,2.7) (1.0,1.85) (2.0,1.3) (3.0,0.95) (4.0,0.74) (5.0,0.62) (6.0,0.55) (7.4,0.47)};
  \node[font=\scriptsize, color=villR, anchor=west] at (3.2,1.65)
      {cold start: from uniform};
  \draw[->, villR!70] (3.15,1.55) -- (2.45,1.18);
  % warm curve
  \draw[very thick, heroG] plot[smooth] coordinates
      {(0.0,1.0) (0.8,0.72) (1.6,0.58) (2.4,0.55) (3.4,0.50) (5.0,0.46) (7.4,0.44)};
  \node[font=\scriptsize, color=heroG, anchor=west] at (4.4,1.0)
      {warm start: from the net's guess $\hat\sigma$};
  \draw[->, heroG!70] (4.35,0.9) -- (3.9,0.56);
  % crossing markers + saved span
  \fill[black!70] (2.4,0.55) circle (1.4pt); \fill[black!70] (6.0,0.55) circle (1.4pt);
  \draw[<->, thick, black!60] (2.4,0.25) -- (6.0,0.25);
  \node[font=\tiny, color=black!60] at (4.2,0.10) {iterations saved};
\end{tikzpicture}
\end{center}

Last natural question: \emph{if the net already knows a near-final strategy,
why not skip CFR and just play $\hat\sigma$?} Because $\hat\sigma$ is an
unverified generalization --- the net has never seen \emph{this exact} PBS,
and playing its raw guess would be exploitable wherever the net happens to
be wrong, with no guarantee left. Warm start is ``trust, then verify'': the
net proposes the usual route, the search drives it against today's actual
traffic.

\begin{intuition}
The two networks accelerate the two axes of the same solver. The
\textbf{value net cuts the tree in space} (leaves replace everything below
the chance node); the \textbf{policy net cuts the solve in time} (start near
the answer instead of at uniform). Remove both and ReBeL degenerates into
exact tabular CFR on the full game --- correct and hopeless. The nets are
not the player; they are compression of past search, spent to make the next
search affordable.
\end{intuition}

\subsection{What the value net actually sees and says}

It is worth being concrete, because the input/output shape \emph{is} the
method. The input is the whole PBS: the public features (board cards, pot,
bets) \emph{plus both belief vectors} --- ${\sim}1326$ probabilities per
player. The output is \textbf{not one number}: it is \textbf{a value per
hand, for each player} (the \emph{infostate values}). It has to be: the
tabular CFR running above the leaves updates a regret \emph{per hand} ---
``what is arriving at this leaf worth \emph{if I hold AA}? \emph{if I hold
76s}?'' --- so a single scalar would be useless to it. Equivalently: the net
is a learned generalization of exactly the per-hand counterfactual values
that DeepStack pioneered, with the beliefs as part of the input so one net
covers every range shape.

The subgame ReBeL solves spans roughly the rest of the current betting
round; wherever new public cards would be revealed, the tree stops and the
net answers. Solving the subgame with $T_{\mathrm{cfr}}$ tabular CFR
iterations queries the net thousands of times --- this is why the net must be
fast, and why its accuracy directly caps the quality of play (the
$\varepsilon_{\mathrm{approx}}$ floor of volume~3).

\subsection{Close-up: the two questions everyone asks}

\textbf{Q1 --- beliefs are computed from the players' strategies. How can
ReBeL know the \emph{opponent's} beliefs?} It cannot, and it does not
pretend to: like Pluribus's ranges, the PBS is a statement about a
\emph{model}. ReBeL computes \emph{all} beliefs --- its own and its
opponent's --- from the assumption that both players follow the policies its
own procedure generates (this is the ``common knowledge'' assumption in the
paper). Against an opponent who actually plays differently, the on-model
logic of Pluribus's Q2 applies verbatim: deviations are, within the model,
unprofitable; early deviations can poison the beliefs; and the next question
is precisely ReBeL's elegant answer to that poisoning attack.

\textbf{Q2 --- after solving the subgame, why play the strategy of a
\emph{randomly sampled} CFR iteration rather than the final average?} This
is the strangest-looking line of the pseudo-code and the paper's quiet
masterstroke; it solves two problems at once.

\begin{itemize}[leftmargin=1.4em, itemsep=2pt]
\item \emph{Training consistency.} The value targets stored for the net are
  values of the subgame's \emph{average} policy --- an average over all
  $T_{\mathrm{cfr}}$ iterations. If play (and hence the belief updates that
  produce the \emph{next} PBS) always followed one fixed choice, the
  distribution of next-PBSs would not match the distribution the stored
  values implicitly assume, and the net would be trained on one
  distribution and queried on another --- compounding off-distribution error
  down the game. Sampling the iteration uniformly makes ``how beliefs are
  generated'' agree, in expectation, with ``how values were averaged''.
\item \emph{Safety.} Recall the hole in naive re-solving (Pluribus close-up,
  Q2): an opponent who knows how you re-solve can poison your beliefs.
  ReBeL's theorem says that playing a uniformly sampled CFR iterate makes
  the depth-limited search \emph{provably} low-exploitability --- the same
  guarantee Libratus needed hand-built gadget games for, obtained instead by
  randomization. Intuition: the opponent cannot tailor an exploit to ``the''
  strategy at the subgame root, because there is no single such strategy ---
  they face the whole cloud of CFR iterates, and beating the cloud on
  average is exactly what CFR's equilibrium guarantee forbids.
\end{itemize}

\begin{intuition}
A useful contrast triangle. \textbf{Libratus}: safety by \emph{construction}
(gadget subgames). \textbf{Pluribus}: safety \emph{dropped} (six-max,
empirical validation). \textbf{ReBeL}: safety by \emph{randomization}
(sample an iterate). All three are answers to the same question --- ``what
stops an opponent from gaming my re-solver?'' --- and the three answers
neatly span engineering, pragmatism, and theory.
\end{intuition}

\textbf{Results.} Superhuman heads-up no-limit hold'em with far less poker
knowledge than DeepStack/Libratus: it beat top HUNL professional Dong Kim by
\textbf{165\,mbb/hand over 7\,500 hands} (Kim was the best-performing human of
the Libratus match). Also solid play in Liar's Dice, showing generality. The
experiments used a fixed menu of bet sizes for tractability; handling
off-tree sizes by nested re-solving (as in Libratus/Pluribus) is compatible
but orthogonal.

\begin{trap}
\textbf{The PBS is the whole trick --- and the whole bill.} The belief vector
needs one probability per private state: 1326 in hold'em is fine; a game with
$10^{10}$ private states is not. And beliefs are only well-defined if the
policy they are computed from is \emph{common knowledge} --- ReBeL effectively
``announces'' its strategy, which is safe at equilibrium but subtle to reason
about away from it. Porting ReBeL to a new game starts with the question:
\emph{can I even write down the belief vector?}
\end{trap}

% ════════════════════════════════════════════════════════════════════════════
\section{Player of Games: one sound algorithm for chess, Go and poker}
% ════════════════════════════════════════════════════════════════════════════

\textbf{Paper:} Schmid et al., \emph{Player of Games}, 2021; published in
Science Advances 2023 under the name \emph{Student of Games}. \quad
\textbf{One idea:} merge AlphaZero's \emph{growing} search tree with CFR's
\emph{sound} handling of hidden information, so the same agent plays both
kinds of games.

\subsection{What ``sound'' buys you}

AlphaZero's MCTS is built for perfect information; applied naively to poker it
converges to exploitable strategies (it has no concept of balancing a range).
ReBeL is sound but poker-shaped. Player of Games (PoG) asks: what is the
\emph{minimal} unification? Its answer has two components:

\begin{itemize}[leftmargin=1.4em, itemsep=2pt]
\item a \textbf{counterfactual value-and-policy network (CVPN)}: given a public
  state plus beliefs (like ReBeL's PBS), it outputs \emph{per-infoset
  counterfactual values} for both players and a policy prior. In a
  perfect-information game the beliefs are trivial and it degrades exactly to
  AlphaZero's value$+$policy net.
\item \textbf{growing-tree CFR (GT-CFR)}: the search keeps a small
  \emph{public} tree and alternates two phases --- \emph{expand} the tree where
  the policy wants to visit (AlphaZero-style), then run \emph{CFR regret
  updates} over the whole current tree with CVPN values at the leaves.
\end{itemize}

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  nd/.style={circle, draw=accent, fill=blue!8, thick, minimum size=5.5mm, inner sep=1pt},
  newnd/.style={circle, draw=heroG, fill=heroG!15, thick, minimum size=5.5mm, inner sep=1pt},
  phase/.style={rectangle, rounded corners=1.5mm, draw=black!50, fill=black!3,
                font=\scriptsize\bfseries, inner sep=3pt}]
  % phase 1: expansion
  \node[phase] at (1.6,4.9) {phase 1: EXPAND (AlphaZero-like)};
  \node[nd] (r1) at (1.6,4.0) {};
  \node[nd] (a1) at (0.6,2.9) {};
  \node[nd] (b1) at (2.6,2.9) {};
  \newcommand{\dummy}{}
  \node[newnd] (c1) at (2.0,1.8) {};
  \node[newnd] (d1) at (3.2,1.8) {};
  \draw[->] (r1) -- (a1); \draw[->] (r1) -- (b1);
  \draw[->, heroG, thick] (b1) -- (c1); \draw[->, heroG, thick] (b1) -- (d1);
  \node[font=\scriptsize, color=heroG, align=center] at (2.6,1.0)
    {new public nodes added\\ where visits concentrate};

  % phase 2: regret update
  \node[phase] at (9.0,4.9) {phase 2: CFR REGRET UPDATE};
  \node[nd] (r2) at (9.0,4.0) {};
  \node[nd] (a2) at (8.0,2.9) {};
  \node[nd] (b2) at (10.0,2.9) {};
  \node[nd] (c2) at (9.4,1.8) {};
  \node[nd] (d2) at (10.6,1.8) {};
  \draw[->] (r2) -- (a2); \draw[->] (r2) -- (b2);
  \draw[->] (b2) -- (c2); \draw[->] (b2) -- (d2);
  % CVPN at leaves
  \node[rectangle, rounded corners=1mm, draw=villR, fill=red!5, font=\scriptsize,
        inner sep=2pt, align=center] (cvpn) at (9.0,0.6)
        {CVPN: counterfactual values at every leaf};
  \draw[->, villR] (a2) .. controls (8.0,1.4) .. (cvpn.west);
  \draw[->, villR] (c2) -- (cvpn);
  \draw[->, villR] (d2) .. controls (10.6,1.0) .. (cvpn.east);
  \node[font=\scriptsize, align=center] at (9.0,-0.3)
    {regret matching on every infoset of the current tree\\
     (this is what keeps the strategy \emph{balanced})};

  \draw[->, very thick, black!60] (4.6,2.9) -- node[above, font=\scriptsize]{alternate} (6.6,2.9);
\end{tikzpicture}
\end{center}

\begin{lstlisting}[style=py, caption={GT-CFR and sound self-play (schematic).}]
def gt_cfr(beliefs, cvpn, n_rounds):
    tree = PublicTree(root=beliefs)            # starts tiny
    for _ in range(n_rounds):
        for _ in range(E):                     # -- EXPAND --
            leaf = descend_by_policy_and_visits(tree)
            tree.add_children(leaf, prior=cvpn.policy(leaf))
        for _ in range(R):                     # -- REGRET UPDATE --
            cfr_iteration(tree, leaf_values=cvpn.values)
    return tree.root_policy(), tree.root_values()

def sound_self_play(cvpn):
    while True:
        traj = play_game(act=lambda b: gt_cfr(b, cvpn, N))
        # every position where the net was queried at a leaf goes into a
        # QUERY BUFFER; a background process re-solves those positions
        # (recursively, with more search) to produce improved value targets
        train(cvpn, value_targets_from(query_buffer), policy_targets_from(traj))
\end{lstlisting}

\begin{intuition}
\textbf{The query buffer is the conceptual heart of the training.} The net is
trained \emph{at exactly the places where the search trusted it} --- each leaf
query becomes a promise to go back later, search deeper from that spot, and
return with a better value to learn from. Compare our project's stage-1 oracle:
the principle ``validate the function where you actually rely on it'' is the
same; PoG just turns it into the training signal itself.
\end{intuition}

\subsection{One GT-CFR round under the microscope: growing a tree}

The listing's two inner loops --- \texttt{EXPAND} then the CFR \texttt{REGRET}
sweep --- are clearest on the same miniature used for ReBeL: each player holds
one of three hand types $A$ (nuts), $B$ (medium), $C$ (air); flop pot 6; a single
bet size; the street ends at the depth limit. What changes from ReBeL is
\emph{what gets built}. ReBeL constructed the \emph{entire} subgame in one shot
and solved all of it; GT-CFR starts from almost nothing and \emph{grows} into the
regions its policy prior cares about. Watch one round.

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=1.8mm]},
  p1/.style={rectangle, draw=heroG, fill=heroG!12, thick, minimum size=5.5mm, inner sep=1pt, font=\scriptsize},
  p2/.style={circle, draw=villR, fill=villR!12, thick, minimum size=5.5mm, inner sep=1pt, font=\scriptsize},
  stub/.style={rectangle, rounded corners=1mm, draw=memO, fill=orange!15, thick, minimum size=4.5mm, font=\tiny, inner sep=1.5pt, align=center},
  done/.style={rectangle, rounded corners=1mm, draw=memO, fill=orange!32, thick, minimum size=4.5mm, font=\tiny, inner sep=1.5pt, align=center},
  el/.style={font=\tiny, fill=white, inner sep=0.5pt}]

  % panel (a)
  \node[p1] (ra) at (0,3.0) {P1};
  \node[stub] (ac) at (-0.9,1.6) {chk\\$\hat\pi{=}.35$};
  \node[stub] (ab) at (0.9,1.6) {bet\\$\hat\pi{=}.65$};
  \draw[->] (ra) -- (ac); \draw[->] (ra) -- (ab);
  \node[font=\scriptsize, align=center] at (0,0.3) {(a) round start:\\ root only,\\ CVPN priors};

  % panel (b)
  \node[p1] (rb) at (5.0,3.0) {P1};
  \node[stub] (bc) at (3.9,1.6) {chk};
  \node[p2]  (bb) at (5.9,1.6) {P2};
  \draw[->] (rb) -- node[el]{c} (bc); \draw[->] (rb) -- node[el]{b} (bb);
  \node[p1]   (bbf) at (5.0,0.3) {P1};
  \node[stub] (bbc) at (6.9,0.3) {call};
  \draw[->] (bb) -- node[el]{b6} (bbf); \draw[->] (bb) -- node[el]{chk} (bbc);
  \node[font=\scriptsize, align=center] at (5.3,-0.9) {(b) EXPAND: the bet spine is\\ realized; check stays a stub};

  % panel (c)
  \node[p1] (rc) at (10.8,3.0) {P1};
  \node[accent, font=\tiny, align=left, anchor=west] at (11.3,3.1) {$\sigma^\ast$: chk .52,\\ bet .48};
  \node[done] (cc) at (9.7,1.6) {chk};
  \node[p2]  (cb) at (11.7,1.6) {P2};
  \draw[->] (rc) -- (cc); \draw[->] (rc) -- (cb);
  \node[p1]   (cbf) at (10.8,0.3) {P1};
  \node[done] (cbc) at (12.7,0.3) {call};
  \draw[->] (cb) -- (cbf); \draw[->] (cb) -- (cbc);
  \node[font=\scriptsize, align=center] at (11.1,-0.9) {(c) REGRET: CFR balances\\ the realized tree};
\end{tikzpicture}
\end{center}

\textbf{Round start (a) --- the tree is a stub.} The only realized node is the
root: P1 to act, pot 6, uniform beliefs. Its two actions are not yet expanded
into children; instead the CVPN, queried at the root PBS, attaches a
\emph{policy prior} $\hat\pi$ and a counterfactual value to each. Say
$\hat\pi=(\text{check }.35,\ \text{bet }.65)$ --- the net's hunch that betting is
the busier branch here.

\textbf{EXPAND (b) --- the AlphaZero move.} To choose \emph{where} to add a node,
PoG descends by the PUCT score, exactly AlphaZero's selection rule:
\[
  \mathrm{score}(a)\;=\;\underbrace{Q(a)}_{\text{value so far}}
  \;+\; c\cdot\hat\pi(a)\cdot\frac{\sqrt{\textstyle\sum_b N(b)}}{1+N(a)} .
\]
With every visit count $N$ still zero the bonus is pure prior, so the \emph{bet}
branch is realized first: a P2 node appears, the CVPN is queried there for its
children's priors and values, and the next expansion descends again into P2's
highest-scoring reply. After $E$ expansions only a thin spine exists --- the
lines the prior thought were live --- while \emph{check} remains an unrealized
stub, valued wholesale by its CVPN counterfactual value. Nothing built the dense
both-branches-to-the-bottom tree ReBeL paid for.

\textbf{REGRET (c) --- the CFR move.} Now run the CFR sweeps over \emph{whatever
tree currently exists}. Every realized internal node carries per-hand regret
tables; every leaf --- a CVPN stub or a fold terminal --- supplies per-hand
values (the stub's from the net, belief-aware, re-priced each sweep exactly as in
ReBeL). Regret matching at the root slides P1's \emph{actual} play from the
prior's $.65$ bet toward whatever this tree's values justify --- here $.48$ ---
balancing the range so P2 stays indifferent. The prior proposed \emph{where to
look}; CFR decided \emph{what to play}.

\textbf{Alternate, and the tree thickens where it matters.} The next round's
expansions use the freshly updated visits and values, so nodes accrete on the
branches CFR keeps returning to while the perpetually-ignored lines stay stubs.
Over many rounds the realized tree is a sparse skeleton hugging the relevant
lines --- the structure that lets the same code survive chess's thirty-way
branching, where ReBeL's build-it-all step is a non-starter.

\begin{intuition}
\textbf{Two solvers, one tree, divided by question.} The expansion loop is
AlphaZero asking \emph{``which branch is worth a node?''} --- visit- and
prior-driven, argmax-flavoured. The regret loop is CFR asking \emph{``what
frequencies here are unexploitable?''} --- mixing, regret-driven. They share the
one growing public tree. Delete the expansion loop and you are back to ReBeL's
dense subgame; delete the regret loop and you are back to AlphaZero, exploitable
the instant hidden information matters.
\end{intuition}

\subsection{What a node of the search tree actually is}

Same exercise as for ReBeL, because here too the data shapes \emph{are} the
method. A node of PoG's tree is a \emph{public} state \textbf{plus the
players' belief vectors} --- a PBS in ReBeL's vocabulary. The CVPN takes
that in and emits two things \emph{per hand}: a counterfactual value (used
when the node is a search leaf) and a policy prior (used to decide where to
grow the tree, exactly like AlphaZero's policy head guides PUCT). Now run
the degeneration test: in chess, the ``belief vectors'' are trivial (one
world state, probability $1$), the public tree \emph{is} the game tree, the
counterfactual values collapse to ordinary state values --- and PoG's
machinery reduces to AlphaZero's value-plus-policy search. The
imperfect-information generality costs nothing when there is nothing to be
uncertain about. That is the precise sense in which it is ``one algorithm
for both kinds of games''.

The training loop runs two processes. \emph{Actors} play games with a modest
search budget, logging every leaf position where the CVPN was queried into
the \textbf{query buffer}. A \emph{trainer} pops queried positions and
re-solves them with a \emph{larger} budget, producing better values than the
net originally returned --- those become its regression targets (and the
deeper solves may query new leaves, which re-enter the buffer with decaying
probability: a self-improvement ladder in which the network forever chases
its own deeper searches).

\subsection{What the CVPN actually sees and says}

As with ReBeL, the input/output shape \emph{is} the method --- with one revealing
twist. ReBeL used \emph{two separate} networks ($\theta_v$ for leaf values,
$\theta_\pi$ for warm-start priors). PoG fuses them into \textbf{one network with
two heads}, exactly AlphaZero's value-plus-policy trunk:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  box/.style={rectangle, rounded corners=1.5mm, thick, align=center, font=\scriptsize, minimum height=12mm}]
  \node[box, draw=memO, fill=orange!10, minimum width=33mm] (in) at (0,0)
    {PBS in:\\ board, pot, bets\\ \emph{both} belief vectors};
  \node[box, draw=villR, fill=red!5, minimum width=20mm] (net) at (4.7,0)
    {CVPN\\ shared trunk};
  \node[box, draw=accent, fill=blue!6, minimum width=42mm] (v) at (10.2,1.05)
    {head 1 --- \emph{values} per hand,\\ both players (used at search leaves)};
  \node[box, draw=heroG, fill=heroG!10, minimum width=42mm] (p) at (10.2,-1.05)
    {head 2 --- \emph{policy prior} per hand\\ (steers where the tree grows)};
  \draw[->, very thick] (in) -- (net);
  \draw[->, very thick] (net.east) -- (v.west);
  \draw[->, very thick] (net.east) -- (p.west);
  \node[font=\scriptsize\itshape, align=center] at (4.7,-1.75)
    {chess: beliefs collapse to a\\ point mass $\Rightarrow$ AlphaZero's two heads};
\end{tikzpicture}
\end{center}

\textbf{Why fuse what ReBeL split?} Both outputs are functions of the same PBS,
so a shared trunk is natural --- and, decisively, in PoG the policy head is
\emph{load-bearing}: it steers tree growth (the PUCT prior), not merely a warm
start, so it must be as good as the value head and is worth training jointly.
ReBeL kept its two apart because its signals are genuinely different --- the value
net learns by \emph{regression} onto solve outputs, the policy net by
\emph{distillation} of average strategies --- and its policy net is only an
accelerator the search may overrule. Same two quantities; different division of
labour.

\textbf{The degeneration test, in I/O terms.} Set the game to chess: the belief
vector is a point mass (one world state, probability $1$), so the ``per-hand''
value vector collapses to a single scalar and the ``per-hand'' prior to a single
move distribution --- and the CVPN \emph{is} AlphaZero's
$(\text{value},\,\text{policy})$ net, bit for bit. Hidden-information generality
costs nothing when the hand is public; that collapse is the whole content of
``one algorithm''.

\subsection{Two phrases unpacked}

\textbf{``Grows a sparse tree'' --- by what rule, exactly?} The same PUCT
selection AlphaZero uses, with one swap. Visits flow to actions scoring high on
$Q(a)+c\,\hat\pi(a)\sqrt{\sum_b N(b)}/(1+N(a))$ --- high prior, high value so far,
few visits spent. The swap: the $Q(a)$ feeding that score is \emph{not} an MCTS
average of rollout returns; it is the value of $a$ under the \emph{CFR solution of
the current tree} --- belief-aware, mixed, sound. Growth is steered by
AlphaZero's bandit arithmetic but fed by CFR's values, which is why expanding the
tree never quietly biases it toward a single deterministic line the way pure MCTS
would.

\textbf{``Sound'' --- a guarantee about what?} Precisely this: with a
bounded-error CVPN, GT-CFR's exploitability is bounded and \emph{shrinks as the
tree grows}. That is the imperfect-information analogue of ``searching one ply
deeper cannot hurt.'' The contrast that gives it teeth: in imperfect information
\emph{plain MCTS has no such property} --- more visits make its play more
deterministic (Q2 below), which can make it \emph{more} exploitable. ``Sound'' is
exactly the guarantee MCTS forfeits the moment hidden information appears, and the
one GT-CFR buys back by doing its searching with CFR.

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]},
  act/.style={rectangle, rounded corners=1.5mm, draw=accent, fill=blue!6, thick, align=center, font=\scriptsize, minimum height=8mm},
  buf/.style={rectangle, rounded corners=1.5mm, draw=memO, fill=orange!10, thick, align=center, font=\scriptsize, minimum height=8mm},
  tr/.style={rectangle, rounded corners=1.5mm, draw=villR, fill=red!5, thick, align=center, font=\scriptsize, minimum height=8mm}]
  \node[act] (a) at (0,0) {actor plays,\\ small search budget};
  \node[buf] (b) at (4.4,0) {query buffer:\\ each trusted leaf PBS};
  \node[tr]  (c) at (9.1,0) {trainer re-solves it\\ with a \emph{larger} budget};
  \node[act, draw=heroG, fill=heroG!12] (d) at (9.1,-2.1) {sharper value target\\ $\Rightarrow$ refit CVPN};
  \draw[->, thick] (a) -- node[above, font=\tiny]{log leaves} (b);
  \draw[->, thick] (b) -- node[above, font=\tiny]{pop} (c);
  \draw[->, thick] (c) -- (d);
  \draw[->, thick, heroG] (d.west) .. controls (3.2,-2.1) and (0,-1.2) ..
      node[below, font=\tiny, pos=0.55]{better net $\Rightarrow$ better next search} (a.south);
  \draw[->, dashed, memO] (c.north) .. controls (11.1,1.5) and (6.7,1.5) ..
      node[above, font=\tiny]{deeper solves spawn new leaves (decaying prob.)} (b.north);
\end{tikzpicture}
\end{center}

The query buffer is what \emph{earns} the bounded error. Every leaf the actor's
modest search trusted is logged; the trainer re-solves it with a larger budget,
returns a sharper value, and the net regresses toward it --- and those deeper
solves query their \emph{own} leaves, which re-enter the buffer with decaying
probability. The network forever chases values produced by more search than it
was allowed at play time: a ladder whose every rung is ``be right where you were
relied upon''.

\begin{trap}
\textbf{Soundness is conditional, and the bill is the belief vector.} Two caveats
travel together. First, the guarantee is \emph{given} bounded value error --- a
CVPN that generalizes badly to an unseen PBS breaks the premise, not merely the
play. Second, PoG inherits ReBeL's entry fee: it must write down a belief vector
per public state, so a game with astronomically many private states, or no clean
notion of ``public state'', is out of reach. ``One algorithm for chess, Go and
poker'' is not ``one algorithm for every game''.
\end{trap}

\subsection{Close-up: the two questions everyone asks}

\textbf{Q1 --- ReBeL also searches over belief states. What is actually
different?} The \emph{shape of the searched region}. At every decision,
ReBeL builds the \textbf{full, dense} subgame down to its depth limit and
solves all of it with tabular CFR --- affordable in poker, where a betting
round has a handful of public actions, but hopeless in games with large
public branching or long horizons between chance events. PoG instead
\textbf{grows a sparse tree} one node at a time, AlphaZero-style: the policy
prior and visit counts decide which few branches deserve nodes at all,
and the CFR phase keeps whatever tree currently exists balanced. Sparse
growth is what lets the same code play chess (huge branching, no beliefs)
and Scotland Yard (long imperfect-information horizons) --- and it is why
PoG, not ReBeL, is the one that generalized beyond poker.

\textbf{Q2 --- why does plain AlphaZero/MCTS fail at poker in the first
place?} Because MCTS is an \emph{argmax engine}: as visits accumulate, it
concentrates on the single best-looking action, and its play at a state
becomes essentially deterministic. In perfect information that is exactly
right --- some move \emph{is} best. In poker, determinism is a tell. A
deterministic policy at a bluffing spot either always bluffs or never
bluffs, and either way an observant opponent prints money (fold always /
call always). Equilibria in imperfect information are \emph{mixed} ---
calibrated frequencies, not best moves --- and producing mixtures is
precisely what regret matching does and tree-search argmax does not. GT-CFR
is therefore not a hybrid for hybrid's sake: the expansion phase brings
MCTS's scalability, the regret phase brings CFR's balance, and each
compensates the other's failure mode.

\begin{intuition}
One sentence to keep: \emph{AlphaZero asks ``what is the best move here?'';
CFR asks ``what frequencies keep me unexploitable here?''} --- and Player of
Games is the observation that you can ask the first question about
\emph{where to look} while asking the second about \emph{what to play}.
\end{intuition}

\textbf{Results.} One set of weights per game, same algorithm: expert-level
chess and Go (below a specialized AlphaZero at equal compute --- the price of
generality), beats Slumbot (the strongest open heads-up poker bot) and is not
exploited by a local best-response probe, and defeats the state of the art in
Scotland Yard, a hide-and-seek board game with imperfect information.

\begin{trap}
Soundness here is a statement about the \emph{search}, given good values:
more search cannot make play more exploitable. It is not a global optimality
proof, and in perfect-information games generality costs head-to-head strength
versus a specialist. ``One algorithm for everything'' and ``the best algorithm
for each thing'' remain different targets.
\end{trap}

% ════════════════════════════════════════════════════════════════════════════
\section{AlphaHoldem: what if we drop the game theory entirely?}
% ════════════════════════════════════════════════════════════════════════════

\textbf{Paper:} Zhao, Yan, Li, Li, Xing, \emph{AlphaHoldem: High-Performance
Artificial Intelligence for Heads-Up No-Limit Poker via End-to-End
Reinforcement Learning}, AAAI 2022. \quad
\textbf{One idea:} no CFR, no search, no beliefs --- a single network maps the
observation directly to an action, trained with a carefully stabilized PPO
against a pool of past selves.

\subsection{Architecture: two eyes, one brain}

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  inp/.style={rectangle, rounded corners=1mm, draw=black!55, fill=black!4,
              align=center, font=\scriptsize, inner sep=3pt, minimum width=33mm},
  conv/.style={rectangle, rounded corners=1.5mm, draw=accent, fill=blue!6, thick,
               align=center, font=\scriptsize, minimum width=33mm, minimum height=9mm},
  head/.style={rectangle, rounded corners=1.5mm, draw=heroG, fill=heroG!10, thick,
               align=center, font=\scriptsize, minimum width=24mm, minimum height=8mm}]
  \node[inp]  (ci) at (0,3.4)  {card tensor\\ hole + board as $4{\times}13$ planes};
  \node[inp]  (ai) at (6.4,3.4){action-history tensor\\ bets of both players, per street};
  \node[conv] (cc) at (0,2.0)  {ConvNet (card stream)};
  \node[conv] (ac) at (6.4,2.0){ConvNet (action stream)};
  \node[conv, minimum width=46mm] (mlp) at (3.2,0.6) {merge + MLP trunk};
  \node[head] (pol) at (1.2,-0.8) {policy head\\ (action probs)};
  \node[head] (val) at (5.2,-0.8) {value head\\ (chip EV)};
  \draw[->, thick] (ci) -- (cc);
  \draw[->, thick] (ai) -- (ac);
  \draw[->, thick] (cc.south) .. controls (0,1.2) .. (mlp.west);
  \draw[->, thick] (ac.south) .. controls (6.4,1.2) .. (mlp.east);
  \draw[->, thick] (mlp) -- (pol);
  \draw[->, thick] (mlp) -- (val);
  \node[font=\scriptsize\itshape, anchor=west] at (8.6,2.0)
    {``pseudo-Siamese'': two};
  \node[font=\scriptsize\itshape, anchor=west] at (8.6,1.6)
    {structurally similar streams,};
  \node[font=\scriptsize\itshape, anchor=west] at (8.6,1.2)
    {separate weights};
\end{tikzpicture}
\end{center}

Note what is \emph{absent}: no belief vector, no infoset enumeration, no
regret anything. The action history tensor plays the role our token sequence
plays in this project --- it is the only channel through which the network can
infer what the opponent's range looks like.

Why these particular encodings? The card stream stacks $4{\times}13$ binary
planes (suit $\times$ rank), one group per street: hole cards, flop, turn,
river. On that grid, poker structure is \emph{geometric}: a straight draw is
a run of adjacent \emph{columns}, a flush draw is mass concentrated in one
\emph{row}, a paired board is two cells sharing a column --- exactly the
local patterns small convolutions detect cheaply. The action stream encodes,
per betting round and per player, which action was taken and the normalized
bet size, again as planes, so the same convolutional machinery picks up
betting \emph{patterns} (e.g.\ ``raise--raise'' textures) rather than
treating the history as an opaque flat vector --- the same lesson, in
convolutional form, that pushed this project from a flat 816-dim input to a
token sequence.

\subsection{One observation under the microscope: the two tensors}

Because ``the data shapes are the method'' here too, it pays to encode one
concrete situation by hand. Hero holds $A\spadesuit K\spadesuit$; the flop is
$K\diamondsuit\,7\spadesuit\,2\clubsuit$. The card stream lays this on a
$4\times13$ grid (suit $\times$ rank), lighting one cell per known card:

\begin{center}
\begin{tikzpicture}[font=\scriptsize, x=6.4mm, y=6.4mm]
  \foreach \r in {0,1,2,3} \foreach \c in {0,...,12}
     \draw[black!18] (\c,\r) rectangle ++(1,1);
  \foreach \c/\lab in {0/2,1/3,2/4,3/5,4/6,5/7,6/8,7/9,8/T,9/J,10/Q,11/K,12/A}
     \node[font=\tiny] at (\c+0.5,4.4) {\lab};
  \foreach \r/\lab in {3/$\spadesuit$,2/$\heartsuit$,1/$\diamondsuit$,0/$\clubsuit$}
     \node at (-0.5,\r+0.5) {\lab};
  % hero hole (green): A spade (12,3), K spade (11,3)
  \fill[heroG!55] (12,3) rectangle ++(1,1);
  \fill[heroG!55] (11,3) rectangle ++(1,1);
  % board (orange): K diamond (11,1), 7 spade (5,3), 2 club (0,0)
  \fill[memO!60] (11,1) rectangle ++(1,1);
  \fill[memO!60] (5,3)  rectangle ++(1,1);
  \fill[memO!60] (0,0)  rectangle ++(1,1);
  \foreach \c/\r in {12/3,11/3,11/1,5/3,0/0} \draw[black!45] (\c,\r) rectangle ++(1,1);
  % K-column highlight
  \draw[villR, very thick, rounded corners=1pt] (10.94,0.94) rectangle (12.06,4.06);
  % spade-row highlight
  \draw[accent, very thick, rounded corners=1pt] (-0.06,2.94) rectangle (13.06,4.06);
  % annotations
  \node[villR, font=\tiny, align=left, anchor=west] at (13.4,2.3)
     {K column lit in two rows\\ $=$ \textbf{top pair}\\ (K$\spadesuit$ hole, K$\diamondsuit$ board)};
  \node[accent, font=\tiny, align=left, anchor=west] at (13.4,3.7)
     {three in the $\spadesuit$ row\\ $=$ \textbf{flush texture}};
  % legend
  \fill[heroG!55] (0,-1.0) rectangle ++(0.7,0.7); \node[font=\tiny, anchor=west] at (0.8,-0.65){your hole};
  \fill[memO!60] (3.0,-1.0) rectangle ++(0.7,0.7); \node[font=\tiny, anchor=west] at (3.8,-0.65){board};
\end{tikzpicture}
\end{center}

Read the grid the way a convolution does. The made pair is \emph{two cells
sharing a column} (K of two suits); the flush texture is \emph{mass along one
row} (three spades); a straight draw would be a \emph{run of adjacent columns};
a paired board, two cells in one column. Every poker-relevant pattern is a
\emph{local} shape on this grid --- precisely what small kernels detect cheaply,
and precisely why the cards are planes and not a flat list. One group of planes
per street (hole, flop, turn, river) keeps ``when'' alongside ``what''.

The action stream is the second eye. It encodes, per street and per player, which
action was taken and the normalized bet size, again as planes, so the same
convolutional machinery reads betting \emph{patterns} rather than an opaque flat
history:

\begin{center}
\begin{tikzpicture}[font=\scriptsize, x=15mm, y=6.2mm]
  \node[font=\tiny] at (0.5,4.35) {P1};
  \node[font=\tiny] at (1.5,4.35) {P2};
  \foreach \r/\lab in {3/pre,2/flop,1/turn,0/river}
     \node[font=\tiny, anchor=east] at (-0.06,\r+0.5) {\lab};
  \foreach \r in {0,1,2,3} \foreach \c in {0,1}
     \draw[black!18] (\c,\r) rectangle ++(1,1);
  \fill[accent!12] (0,3) rectangle ++(2,1);
  \node[font=\tiny] at (0.5,3.5) {raise 2.5}; \node[font=\tiny] at (1.5,3.5) {call};
  \node[font=\tiny] at (0.5,2.5) {bet .5p};   \node[font=\tiny] at (1.5,2.5) {call};
  \node[font=\tiny] at (0.5,1.5) {check};     \node[font=\tiny] at (1.5,1.5) {check};
  \node[font=\tiny] at (0.5,0.5) {bet 1p};    \node[font=\tiny] at (1.5,0.5) {--};
  \node[font=\tiny, anchor=west, align=left] at (2.25,2.0)
     {same grid logic: a ``raise--raise''\\ texture becomes a local pattern\\ the conv can name, not an opaque\\ flat history --- this is the implicit\\ channel through which the net infers\\ the opponent's range};
\end{tikzpicture}
\end{center}

\subsection{Making PPO survive poker}

Vanilla PPO collapses on poker: rewards span $\pm$200 big blinds with brutal
variance, and importance ratios explode on rare lines. AlphaHoldem's
\textbf{Trinal-Clip PPO} adds two clamps to the standard clipped loss:

\begin{lstlisting}[style=py, caption={Trinal-Clip PPO (the three clips). delta1 caps the policy ratio when the advantage is negative; delta2/delta3 cap the value target at what is physically winnable/losable in the hand.}]
def trinal_clip_losses(ratio, adv, v_pred, v_target, eps, d1, d2, d3):
    # 1+2) standard PPO clip, PLUS an upper cap d1 (> 1+eps) on the ratio
    #      when adv < 0 -- a huge ratio times a negative advantage otherwise
    #      yields an unbounded, variance-exploding policy loss
    r = clip(ratio, 1 - eps, 1 + eps) if adv >= 0 else \
        clip(ratio, 1 - eps, d1)
    policy_loss = -min(ratio * adv, r * adv)
    # 3) value target clipped to the chips actually reachable this hand
    #    (d2 = max losable, d3 = max winnable -- pot/stack dependent)
    value_loss = (v_pred - clip(v_target, -d2, d3)) ** 2
    return policy_loss, value_loss
\end{lstlisting}

Numbers make the two failure modes concrete. Take a rare line --- the new policy
makes it $8\times$ likelier than the old, so the importance ratio is $\rho=8$ ---
that turned out badly: advantage $A=-150$\,bb.

\textbf{Clip 1 (policy ratio, the $\delta_1$ cap).} The surrogate weights the
update by $\rho\cdot A$. Left unbounded, that single rare transition contributes
$8\times(-150)=-1200$ to the objective --- a gradient spike that swamps the
hundreds of ordinary transitions sharing the batch. Standard PPO's
$[1{-}\epsilon,\,1{+}\epsilon]$ band does not rescue the \emph{negative}-advantage
side; the extra upper cap $\delta_1$ does, by refusing to let $\rho$ count for
more than $\delta_1$ there. The heavy right tail of the ratio histogram is simply
truncated.

\textbf{Clips 2--3 (value target, the reachable band).} A bootstrapped value
target can overshoot what the hand can physically pay. If only $120$\,bb is live
(the pot plus the stacks that can still go in), a regression target of $+400$ is
an impossibility the critic should never chase. Clipping the target to
$[-d_2,\,d_3]=[-120,\,+120]$ --- max losable, max winnable \emph{this} hand ---
keeps value learning inside what the chips allow.

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]}]
  % policy-ratio axis
  \draw[->, thick] (0,1.7) -- (9.4,1.7); \node[font=\scriptsize, anchor=west] at (9.5,1.7) {ratio $\rho$};
  \foreach \x in {1.2,2.4,3.6,6.0} \draw (\x,1.6)--(\x,1.8);
  \node[font=\tiny] at (1.2,2.02){$1{-}\epsilon$}; \node[font=\tiny] at (2.4,2.02){$1$};
  \node[font=\tiny] at (3.6,2.02){$1{+}\epsilon$}; \node[font=\tiny] at (6.0,2.02){$\delta_1$};
  \fill[heroG!25] (1.2,1.62) rectangle (3.6,1.78);
  \node[font=\tiny, heroG, anchor=west] at (1.1,1.42) {standard band ($A\ge0$)};
  \draw[memO, very thick] (3.6,1.7) -- (6.0,1.7);
  \node[font=\tiny, memO, anchor=west] at (3.7,2.34) {extra cap when $A<0$};
  \node[villR, font=\tiny, anchor=west] at (6.55,1.7) {$\rho{=}8$ clamped to $\delta_1$};
  \draw[->, villR] (6.9,1.54) .. controls (6.6,1.02) .. (6.02,1.62);
  % value axis
  \draw[->, thick] (0,-0.5) -- (9.4,-0.5); \node[font=\scriptsize, anchor=west] at (9.5,-0.5) {value target (bb)};
  \foreach \x in {2.0,5.5} \draw (\x,-0.6)--(\x,-0.4);
  \node[font=\tiny] at (2.0,-0.84){$-d_2$}; \node[font=\tiny] at (5.5,-0.84){$+d_3$};
  \fill[heroG!25] (2.0,-0.58) rectangle (5.5,-0.42);
  \node[font=\tiny, heroG] at (3.75,-0.18) {chips reachable this hand};
  \node[villR, font=\tiny, anchor=west] at (6.35,-0.5) {$+400$ clipped to $+d_3$};
  \draw[->, villR] (6.7,-0.66) .. controls (6.3,-1.06) .. (5.55,-0.58);
\end{tikzpicture}
\end{center}

Both clips encode the same physical fact: the chips on the table bound every
learning target. The ratio cap bounds the policy update's reach on a rare line;
the value band bounds the critic's target to what the pot can pay. Poker's heavy
tails are real money, not noise to average away --- but they are finite, and that
finiteness is exactly where it is safe to clip.

\subsection{K-best self-play against strategy cycling}

Naive self-play (always train against the latest self) famously
\emph{cycles} in poker: agent B beats A, C beats B, and C loses to A again ---
rock-paper-scissors in strategy space, no monotone progress. AlphaHoldem
trains each new agent against the \textbf{$K$ best historical versions}
simultaneously, scored by an Elo-like rating; a new agent enters the pool only
if it earns its place. (AlphaStar's league is the same idea at larger scale.)

\begin{lstlisting}[style=py, caption={K-best self-play.}]
pool = [random_init()]
while training:
    learner = clone(best(pool))
    for _ in range(n_steps):
        opponent = sample(pool)                 # mix over the K best
        rollouts = play(learner, opponent)
        update(learner, trinal_clip_ppo(rollouts))
    pool = top_k(pool + [learner], K, by=elo)   # survival of the fittest
\end{lstlisting}

\subsection{The cycle under the microscope: rock--paper--scissors in strategy space}

Q2 below names the disease; a picture makes it unforgettable. Three self-play
checkpoints can chase each other in a directed cycle --- $B$ beats $A$, $C$ beats
$B$, and $A$ beats $C$ --- so head-to-head ``progress'' is a carousel, not a
climb. A naive rating, which only ever sees the latest pairwise result, marches
upward the whole time:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2.4mm]},
  ag/.style={circle, draw=accent, fill=blue!8, thick, minimum size=9mm, font=\scriptsize},
  el/.style={font=\tiny, fill=white, inner sep=0.5pt}]
  \node[ag] (A) at (90:1.55) {$A$};
  \node[ag] (B) at (210:1.55) {$B$};
  \node[ag] (C) at (330:1.55) {$C$};
  \draw[->, very thick, villR] (B) to[bend left=10] node[el]{beats} (A);
  \draw[->, very thick, villR] (C) to[bend left=10] node[el]{beats} (B);
  \draw[->, very thick, villR] (A) to[bend left=10] node[el]{beats} (C);
  \node[font=\tiny, align=center] at (0,-0.05) {a cycle:\\ no winner};
  \begin{scope}[xshift=6.0cm, yshift=-0.2cm]
    \draw[->, thick] (0,-1.4) -- (4.2,-1.4) node[right, font=\scriptsize]{training};
    \draw[->, thick] (0,-1.4) -- (0,1.5);
    \draw[very thick, heroG] (0,-1.1) -- (3.9,1.2);
    \node[heroG, font=\tiny, anchor=west] at (1.45,0.5) {naive Elo (climbs)};
    \draw[very thick, villR, dashed] (0,-0.35) -- (3.9,-0.3);
    \node[villR, font=\tiny, anchor=west] at (1.15,-0.78) {true exploitability (flat)};
  \end{scope}
\end{tikzpicture}
\end{center}

The fix is to make ``entering the pool'' demand strength against a \emph{spread}
of past styles at once --- a crude integral over the orbit rather than a single
point on it. Each new learner trains against, and is rated against, the $K$ best
historical agents; it joins the pool only if its Elo clears the bar:

\begin{center}
\begin{tikzpicture}[font=\small, >={Stealth[length=2mm]},
  slot/.style={rectangle, rounded corners=1mm, draw=accent, fill=blue!8, thick, minimum width=10mm, minimum height=7mm, font=\scriptsize, align=center}]
  \node[font=\scriptsize\bfseries] at (3.0,1.25) {$K$-best pool (ranked by Elo)};
  \node[slot] (s1) at (0,0)   {\#1};
  \node[slot] (s2) at (1.5,0) {\#2};
  \node[slot] (s3) at (3.0,0) {\#3};
  \node[font=\small] at (4.2,0) {$\cdots$};
  \node[slot] (sk) at (5.4,0) {\#$K$};
  \node[slot, draw=heroG, fill=heroG!12] (L) at (2.7,-2.1) {learner};
  \draw[->, thick, heroG] (s1) -- (L);
  \draw[->, thick, heroG] (s3) -- (L);
  \draw[->, thick, heroG] (sk) -- (L);
  \node[font=\tiny, heroG, anchor=west] at (3.7,-1.15) {train vs a \emph{mix} of the $K$};
  \draw[->, thick, villR, dashed] (L.east) .. controls (7.2,-2.1) and (7.2,0) ..
      node[right, font=\tiny, align=left, pos=0.5]{enters only if its\\ Elo clears the bar} (sk.east);
\end{tikzpicture}
\end{center}

This is the same medicine AlphaStar's league used at industrial scale, and ---
as Q2 argues --- it is really CFR's averaging theorem smuggled back in through the
opponent distribution: you cannot carry an explicit average strategy, so you force
the \emph{opponents} to be an average instead.

\subsection{Two phrases unpacked}

\textbf{``Pseudo-Siamese'' --- twins, but not identical.} A true Siamese network
runs \emph{one} set of weights over two inputs of the \emph{same} kind (two faces,
say) to compare them. AlphaHoldem's two streams share only the \emph{shape} --- a
small ConvNet over a grid --- not the \emph{weights}, because their grids mean
different things: a kernel that learned to read a flush draw as mass along a suit
row would be nonsense slid across the action-history planes, whose axes are
streets and players. ``Pseudo'' is the admission that the symmetry is
architectural, not semantic --- structurally twinned feature extractors,
separately trained.

\textbf{``End-to-end'' --- what was deleted, and what stands in.} Lay the volume's
machinery beside AlphaHoldem and read off the subtractions:

\begin{itemize}[leftmargin=1.4em, itemsep=2pt]
\item \textbf{ranges / beliefs --- gone.} No PBS, no infoset enumeration. The
  action-history ConvNet must \emph{implicitly} infer ``what does the opponent's
  range look like'' from the betting planes alone.
\item \textbf{the search tree --- gone.} No subgame, no CFR-at-play-time. The
  single forward pass \emph{is} the decision (2.9\,ms, Results below).
\item \textbf{regret and averaging --- gone.} No explicit average strategy. The
  PPO gradient plus the K-best pool stand in for CFR's averaging theorem, as the
  cycle picture above makes literal.
\end{itemize}

And yet poker knowledge does not vanish --- it relocates. The value clip's
reachable band, the $4\times13$ card geometry, the per-street action planes, the
K-best schedule: each is domain-specific scaffolding doing work that CFR's
structure used to do for free. ``End-to-end'' describes the unbroken gradient path
from observation to action, not an absence of poker priors.

\begin{trap}
\textbf{``End-to-end'' hides its inductive biases in the plumbing.} It is tempting
to read AlphaHoldem as ``just let SGD figure out poker''. But the encodings and
the clips \emph{are} the priors that make the gradient well-behaved; swap the card
grid for a flat vector, or drop the reachable-chips value clip, and the same PPO
diverges. The lesson for this project is the mirror image of its own history: the
move from a flat 816-dim input to a token sequence was exactly this kind of
inductive-bias engineering, and it mattered every bit as much as the learning
rule.
\end{trap}

\subsection{Close-up: the two questions everyone asks}

\textbf{Q1 --- no search, no ranges: where does balance (bluffing) come
from, if anywhere?} Only from gradient pressure. If the current policy is
predictable --- bets only its strong hands --- then \emph{provided some
opponent in the K-best pool punishes that pattern}, those rollouts lose
money and the policy gradient pushes toward mixing. Balance can emerge, but
it is an \emph{equilibrium of the pool}, not of the game: nothing certifies
that the pool contains the punisher for every leak. Contrast the other
systems in this volume: Pluribus \emph{computes} balance per spot (Trick~2,
the whole-range solve); AlphaHoldem \emph{hopes SGD finds it}. This is the
precise content of the ``no certificate'' trade --- and why follow-up work
exploiting pure-RL poker agents keeps finding leaks that self-play ratings
never showed.

\textbf{Q2 --- why does naive self-play cycle, and why does a K-best pool
help?} Volume 3 proved the deep reason: in zero-sum games, the
\emph{current} strategies of self-play never converge --- they orbit the
equilibrium forever (the RPS table showed a 73\%-Paper current strategy
mid-convergence); only the \emph{average} converges. CFR is built around
that fact: it carries the average explicitly. A vanilla RL agent keeps only
a current policy, so it \emph{lives} the orbit instead of averaging it:
B learns to beat A, C learns to beat B, and C loses to A --- rock--paper--%
scissors in strategy space, with ratings that go up forever while true
strength goes nowhere. Training against the $K$ best \emph{historical}
agents at once is a crude integral over the orbit: to enter the pool you
must do well against a \emph{spread} of past play styles simultaneously,
which damps the cycle --- a poor man's strategy averaging. (AlphaStar's
league is the same medicine at industrial scale.) Seen this way, K-best
self-play is not an RL hack; it is CFR's averaging theorem, smuggled back in
through the opponent distribution.

\begin{intuition}
The two clips of Trinal-Clip PPO have the same shape as this project's
regret-scale reasoning: bound every learning target by what is
\emph{physically reachable in the hand} ($\pm$ the committed chips). Heavy
tails in poker are not noise to average away --- they are impossible targets
to refuse.
\end{intuition}

\textbf{Results.} Over 100\,000 evaluation hands, AlphaHoldem beats Slumbot
and a DeepStack re-implementation, after \textbf{3 days of training on one
8-GPU server}, and then decides in \textbf{2.9\,ms} on a single GPU ---
roughly a thousand times faster than search-based systems.

\begin{trap}
\textbf{No theory, no certificate.} Nothing bounds AlphaHoldem's
exploitability; ``beats Slumbot'' does not imply ``cannot be exploited by a
strategy that hunts for its leaks'' --- and follow-up work has indeed found
pure-RL poker agents exploitable. This is the precise trade this corner of the
design-space map makes: spectacular speed and simplicity, no safety net. Our
project's self-play collapses (the 1M-step incoherent policy) were a small
taste of the same phenomenon --- and our stage-1 Nash oracle is exactly the
kind of certificate AlphaHoldem lacks.
\end{trap}

% ════════════════════════════════════════════════════════════════════════════
\section{Side by side, and what this project takes from each}
% ════════════════════════════════════════════════════════════════════════════

\begin{center}
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
 & Deep CFR & Pluribus & ReBeL & PoG/SoG & AlphaHoldem \\
\midrule
year                 & 2019 & 2019 & 2020 & 2021 & 2022 \\
core engine          & CFR  & CFR  & CFR+RL & CFR+MCTS & PPO \\
neural nets          & value+policy & \textbf{none} & value+policy & CVPN & policy+value \\
search at play time  & no   & yes  & yes  & yes  & no \\
belief states needed & no   & no   & yes  & yes  & no \\
abstraction needed   & no   & yes  & no   & no   & no \\
players              & 2    & \textbf{6} & 2 & 2 & 2 \\
theoretical target   & Nash & none (empirical) & Nash & sound search & none \\
training hardware    & GPUs & 64 CPU cores, 8 days & many GPUs & TPUs & 8 GPUs, 3 days \\
decision latency     & ms   & seconds & seconds & seconds & \textbf{2.9 ms} \\
\bottomrule
\end{tabular}
\end{center}

\begin{intuition}
\textbf{Choosing for this project.} PokerTrainer sits in the Deep CFR cell on
purpose: no abstraction to hand-craft (the token transformer reads raw
infosets), no play-time search infrastructure (one network ships in the
Flutter inspector and the C ABI), and a real theoretical target (Nash) that
gives us measurable progress --- which the stage-1 push/fold oracle exploits
directly. The map's other corners tell us what we are giving up: Pluribus-style
re-solving would buy precision at the cost of an online solver; ReBeL/PoG
belief machinery would buy soundness-at-depth at the cost of 1326-dim belief
plumbing; AlphaHoldem's pure RL would buy simplicity at the cost of the very
guarantees we built the oracle to enforce.
\end{intuition}

Concrete techniques worth borrowing here, in increasing order of effort:

\begin{enumerate}[leftmargin=1.6em, itemsep=2pt]
\item \textbf{K-best opponent pools (AlphaHoldem)} --- our evaluation matches
  already revealed self-play drift; rating checkpoints with Elo and evaluating
  new ones against the $K$ best is cheap and directly transplantable.
\item \textbf{Reward/value clipping by reachable chips (AlphaHoldem)} --- the
  same physical bounds ($\pm$ effective stack) we used to reason about regret
  scale apply to any value-learning component we add.
\item \textbf{Continuation-strategy leaf evaluation (Pluribus)} --- if we ever
  add depth limits beyond the current crude bootstrap (flagged in
  \texttt{cfr\_coro.py}), the 4-continuation construction is the principled
  upgrade.
\item \textbf{Query-buffer-style targeted validation (PoG)} --- extend the
  stage-1 oracle idea: log the infosets where the AdvNet's regrets most
  influenced traversals, and audit/validate exactly there.
\end{enumerate}

% ════════════════════════════════════════════════════════════════════════════
\section{Further reading}
% ════════════════════════════════════════════════════════════════════════════

\begin{itemize}[leftmargin=1.4em, itemsep=1pt]
\item Brown, Sandholm (2019). \emph{Superhuman AI for multiplayer poker}
  (Pluribus). Science.
\item Brown, Sandholm (2018). \emph{Depth-Limited Solving for
  Imperfect-Information Games} --- the continuation-strategies construction
  Pluribus builds on.
\item Brown, Bakhtin, Lerer, Gong (2020). \emph{Combining Deep Reinforcement
  Learning and Search for Imperfect-Information Games} (ReBeL). NeurIPS.
\item Schmid et al.\ (2021/2023). \emph{Player of Games} / \emph{Student of
  Games: A unified learning algorithm for both perfect and imperfect
  information games}. Science Advances.
\item Zhao, Yan, Li, Li, Xing (2022). \emph{AlphaHoldem: High-Performance
  Artificial Intelligence for Heads-Up No-Limit Poker via End-to-End
  Reinforcement Learning}. AAAI.
\item Morav\v{c}\'{i}k et al.\ (2017). \emph{DeepStack} --- the origin of
  learned counterfactual values at depth limits, ancestor of both ReBeL and
  PoG.
\item Companion volume: \emph{Deep CFR, explained}
  (\texttt{docs/deep\_cfr\_tutorial/}) --- regret matching, CFR, MCCFR,
  Deep CFR, and this project's curriculum.
\end{itemize}

\end{document}
